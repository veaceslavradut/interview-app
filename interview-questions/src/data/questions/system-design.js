// Категория: system-design
// Оригинальный набор вопросов по проектированию высоконагруженных систем
// (System Design). Ответы — markdown; у каждого вопроса проставлены сложность,
// теги и связанные вопросы (item 14).
export const systemDesign = {
  id: 'system-design',
  title: 'System Design',
  icon: '🏗️',
  description: 'Проектирование высоконагруженных распределённых систем',
  questions: [
    {
      id: 'sd-approach',
      difficulty: 'easy',
      tags: ['basics'],
      related: ['sd-requirements', 'sd-estimation', 'sd-url-shortener'],
      question: 'Как подходить к задаче System Design на собеседовании?',
      answer: `Задача намеренно сформулирована расплывчато — интервьюер смотрит на **ход мыслей**, а не на «единственно верный» ответ. Двигайтесь по структуре:

1. **Уточните требования** — функциональные (что система делает) и нефункциональные (нагрузка, задержка, доступность, согласованность). Не проектируйте вслепую.
2. **Оценка масштаба** — прикиньте QPS, объём хранения, трафик «на салфетке». Числа определяют архитектуру.
3. **API** — опишите основные эндпоинты (вход/выход). Это фиксирует контракт.
4. **Модель данных** — какие сущности, где хранятся (SQL/NoSQL/блоб-хранилище).
5. **High-level дизайн** — нарисуйте компоненты: клиенты, балансировщик, сервисы, кэш, БД, очереди.
6. **Углубление (deep dive)** — проработайте 1–2 узких места, которые важны интервьюеру.
7. **Узкие места и компромиссы** — единые точки отказа, горячие ключи, согласованность vs доступность.

Главное правило: **проговаривайте вслух компромиссы**. «Здесь я выберу eventual consistency ради доступности, потому что…» ценится выше, чем молчаливое рисование квадратиков.`,
    },
    {
      id: 'sd-requirements',
      difficulty: 'easy',
      tags: ['basics'],
      related: ['sd-approach', 'sd-estimation'],
      question: 'Чем отличаются функциональные и нефункциональные требования?',
      answer: `**Функциональные требования** описывают, **что** система делает — конкретное поведение и фичи:

- «пользователь может сократить ссылку и получить короткий код»;
- «по короткому коду происходит редирект на оригинал».

**Нефункциональные требования (NFR)** описывают, **насколько хорошо** система это делает, — и именно они формируют архитектуру:

- **Масштаб/нагрузка** — сколько запросов в секунду, пользователей, данных;
- **Задержка (latency)** — целевые p50/p99;
- **Доступность (availability)** — «три девятки» (99.9%) и т.д.;
- **Согласованность (consistency)** — сильная или eventual;
- **Долговечность (durability)** — нельзя терять данные;
- **Безопасность, стоимость, наблюдаемость**.

На интервью сначала соберите функциональные требования (2–3 ключевых), затем NFR — они подскажут, нужен ли кэш, шардирование, репликация, очереди.`,
    },
    {
      id: 'sd-estimation',
      difficulty: 'medium',
      tags: ['basics', 'estimation'],
      related: ['sd-approach', 'sd-requirements'],
      question: 'Что такое оценка «на салфетке» (back-of-the-envelope) и зачем она нужна?',
      answer: `Это **грубая прикидка** масштаба системы по порядку величины — чтобы обосновать архитектурные решения числами, а не интуицией.

Что считают:

- **QPS** — запросов в секунду. Из «DAU × действий в день / 86 400с» получают средний QPS; пиковый обычно берут ×2–×5. Отдельно считают read и write QPS (обычно чтений на порядок больше).
- **Хранение** — размер записи × число записей × срок хранения. Не забыть про репликацию (×3) и рост.
- **Трафик (bandwidth)** — QPS × средний размер ответа.
- **Память под кэш** — по правилу 80/20 в кэш кладут «горячие» ~20% данных.

Полезные ориентиры: в сутках ≈ **86 400 секунд** (≈ 100 тыс.); 1 млн DAU при 10 действиях ≈ ~115 QPS в среднем.

Цель — не точность до процента, а понять, «влезаем ли в один сервер или нужен кластер, кэш и шардирование». Всегда проговаривайте допущения вслух.`,
    },
    {
      id: 'sd-scaling',
      difficulty: 'easy',
      tags: ['scalability'],
      related: ['sd-load-balancing', 'sd-sharding'],
      question: 'В чём разница между вертикальным и горизонтальным масштабированием?',
      answer: `**Вертикальное (scale up)** — увеличить мощность одной машины (CPU, RAM, диск).

- ➕ просто, не требует изменения кода, нет распределённых проблем;
- ➖ есть физический потолок, дорого на верхних уровнях, машина — единая точка отказа.

**Горизонтальное (scale out)** — добавить больше машин и распределить нагрузку между ними.

- ➕ практически неограниченно, отказоустойчивость (одна нода упала — остальные работают), дешевле на commodity-железе;
- ➖ сложнее: нужны балансировщик, распределение состояния, согласованность данных, борьба с сетевыми отказами.

Практика: до определённого масштаба проще scale up, но по-настоящему большие системы масштабируются **горизонтально**. Ключевое условие для scale out — **stateless-сервисы** (состояние вынесено в БД/кэш), тогда любой запрос может обработать любая нода.`,
    },
    {
      id: 'sd-load-balancing',
      difficulty: 'medium',
      tags: ['scalability', 'networking'],
      related: ['sd-scaling', 'sd-observability'],
      question: 'Что такое балансировщик нагрузки и какие бывают алгоритмы/уровни?',
      answer: `**Балансировщик (load balancer)** распределяет входящие запросы между несколькими серверами, чтобы не перегрузить один и обеспечить отказоустойчивость.

**Уровни:**

- **L4 (транспортный)** — балансирует по IP/портам (TCP/UDP), не заглядывая в содержимое. Быстрый, но «тупой».
- **L7 (прикладной)** — понимает HTTP: может маршрутизировать по URL, заголовкам, куки; терминировать TLS. Гибче, но дороже.

**Алгоритмы:**

- **Round Robin** — по кругу; простой, но не учитывает загрузку;
- **Least Connections** — на сервер с наименьшим числом активных соединений;
- **Weighted** — с учётом «веса» (мощности) сервера;
- **IP/hash-based** — стабильно направляет клиента на один сервер (нужно для сессий).

Важные детали: **health checks** (исключать упавшие ноды), избегание единой точки отказа (сам балансировщик резервируют), а также **sticky sessions** vs вынос состояния наружу (предпочтительно stateless).`,
    },
    {
      id: 'sd-caching',
      difficulty: 'medium',
      tags: ['caching'],
      related: ['sd-cdn', 'sd-consistency-models'],
      question: 'Какие есть стратегии кэширования и вытеснения?',
      answer: `**Кэш** — быстрое хранилище «горячих» данных, чтобы разгрузить БД и снизить задержку.

**Стратегии записи/чтения:**

- **Cache-aside (lazy loading)** — приложение сначала смотрит в кэш; промах → читает из БД и кладёт в кэш. Самая частая. Минус: первый запрос медленный, данные могут устареть.
- **Write-through** — пишем одновременно в кэш и БД. Кэш всегда свежий, но запись медленнее.
- **Write-back (write-behind)** — пишем в кэш, а в БД асинхронно. Быстро, но риск потери данных при сбое.
- **Read-through** — кэш сам подгружает данные из БД при промахе (логика в кэш-слое).

**Вытеснение (eviction):**

- **LRU** — вытесняем давно не использованное (самое популярное);
- **LFU** — вытесняем редко используемое;
- **FIFO/TTL** — по времени жизни.

**Ключевые проблемы:** инвалидация («одна из двух сложных задач в CS»), **cache stampede** (лавина запросов при массовом истечении TTL — лечится джиттером/блокировкой), консистентность кэша и БД.`,
    },
    {
      id: 'sd-cdn',
      difficulty: 'easy',
      tags: ['caching', 'networking'],
      related: ['sd-caching'],
      question: 'Что такое CDN и когда он нужен?',
      answer: `**CDN (Content Delivery Network)** — географически распределённая сеть кэширующих серверов (edge-нод), которая отдаёт контент из точки, ближайшей к пользователю.

**Зачем:**

- снижает **задержку** — данные ближе к пользователю физически;
- разгружает **origin-сервер** — большинство запросов обслуживает edge;
- сглаживает пики и частично защищает от DDoS.

**Что кладут в CDN:** статику (картинки, CSS/JS, видео, файлы). Динамику — реже, но edge-логика и кэширование API-ответов тоже возможны.

**Как работает:** запрос идёт на ближайший edge; если контент там есть (**cache hit**) — отдаётся сразу; если нет (**miss**) — edge тянет с origin, кэширует и отдаёт. Управление свежестью — через **TTL** и заголовки \`Cache-Control\`, а также инвалидацию/версионирование по имени файла (\`app.abc123.js\`).

Нужен, когда есть заметная доля статики и/или географически распределённая аудитория.`,
    },
    {
      id: 'sd-cap',
      difficulty: 'hard',
      tags: ['consistency', 'databases'],
      related: ['sd-consistency-models', 'sd-sql-vs-nosql', 'sd-replication'],
      question: 'Сформулируйте CAP-теорему. Что такое PACELC?',
      answer: `**CAP-теорема:** в распределённой системе при **сетевом разделении (Partition)** нельзя одновременно гарантировать и **согласованность (Consistency)**, и **доступность (Availability)** — приходится жертвовать одним.

- **C** — все узлы видят одни и те же (самые свежие) данные;
- **A** — каждый запрос получает ответ (не ошибку), пусть и не самый свежий;
- **P** — система продолжает работать при потере сообщений между узлами.

Поскольку в реальной сети разделения **неизбежны**, P — не выбор, а данность. Реальный выбор происходит **во время** разделения:

- **CP-системы** — жертвуют доступностью: при разделении часть узлов отвечает ошибкой, чтобы не отдать устаревшие данные (напр. этого требуют банковские сценарии);
- **AP-системы** — жертвуют согласованностью: отвечают всегда, но данные могут быть устаревшими (лента соцсети).

**PACELC** уточняет: **если** есть Partition — выбор между A и C; **иначе (Else)**, в нормальном режиме, — выбор между **латентностью (L)** и **согласованностью (C)**. То есть согласованность стоит задержки даже без сбоев.`,
    },
    {
      id: 'sd-consistency-models',
      difficulty: 'hard',
      tags: ['consistency'],
      related: ['sd-cap', 'sd-replication'],
      question: 'Какие бывают модели согласованности?',
      answer: `Модель согласованности определяет, **какие гарантии** о свежести данных получает читатель после записи.

- **Сильная (strong)** — после успешной записи любое последующее чтение видит новое значение. Удобно рассуждать, но дороже по латентности и хуже с доступностью (CP).
- **Eventual (итоговая)** — если новых записей нет, реплики *со временем* сходятся к одному значению. Читатель может временно увидеть старое. Дёшево и высокодоступно (AP) — годится для лайков, счётчиков просмотров.
- **Read-your-writes** — пользователь всегда видит **свои** последние записи (даже если другие ещё нет). Важно для UX: отправил комментарий — сразу его видишь.
- **Monotonic reads** — читатель не «откатывается» назад во времени: увидев версию N, не увидит потом N-1.
- **Causal** — сохраняется причинно-следственный порядок: ответ не появится раньше вопроса.

Выбор — компромисс: сильная согласованность упрощает логику, но снижает доступность и увеличивает задержку; eventual — наоборот. Часто в одной системе разные данные имеют разные модели.`,
    },
    {
      id: 'sd-sql-vs-nosql',
      difficulty: 'medium',
      tags: ['databases'],
      related: ['sd-cap', 'sd-sharding', 'sd-replication'],
      question: 'Как выбрать между SQL и NoSQL?',
      answer: `**SQL (реляционные)** — строгая схема, связи, транзакции **ACID**, мощный язык запросов и JOIN.

- Хорош, когда важны целостность и сложные запросы: финансы, заказы, отчётность.
- Масштабируется в основном **вертикально**; горизонтально — сложнее (шардирование, read-реплики).

**NoSQL** — общий термин для нереляционных хранилищ, обычно с гибкой схемой и упором на горизонтальное масштабирование и доступность (часто **BASE** вместо ACID):

- **key-value** (Redis, DynamoDB) — простые быстрые доступы по ключу;
- **документные** (MongoDB) — гибкие JSON-документы;
- **колоночные** (Cassandra) — огромные объёмы записи, широкие строки;
- **графовые** (Neo4j) — связи как первоклассная сущность.

**Как выбирать:**

- нужны транзакции, связи, сложные запросы, строгая согласованность → **SQL**;
- нужен огромный масштаб, простые паттерны доступа, гибкая схема, высокая доступность → **NoSQL**;
- часто применяют **и то и другое** (polyglot persistence): SQL для «истины», NoSQL/кэш для скорости и масштаба.

Ключевой тезис на интервью: выбор диктуется **паттернами доступа и требованиями к согласованности**, а не модой.`,
    },
    {
      id: 'sd-replication',
      difficulty: 'medium',
      tags: ['databases', 'reliability'],
      related: ['sd-sharding', 'sd-consistency-models'],
      question: 'Что такое репликация БД и какие есть схемы?',
      answer: `**Репликация** — хранение копий данных на нескольких узлах ради **доступности**, **отказоустойчивости** и масштабирования чтений.

**Схемы:**

- **Leader–Follower (master–slave)** — записи идут в лидера, он реплицирует их на фолловеров; чтения можно распределять по фолловерам. Просто, но лидер — узкое место для записи и точка отказа (нужен **failover** — повышение фолловера в лидеры).
- **Multi-Leader** — несколько узлов принимают записи (напр. в разных ЦОД). Выше доступность записи, но появляются **конфликты записи**, которые надо разрешать.
- **Leaderless** (Dynamo-стиль, Cassandra) — запись/чтение идут на несколько узлов с **кворумом** (W + R > N гарантирует пересечение). Высокая доступность, eventual consistency.

**Синхронная vs асинхронная:** синхронная реплика подтверждает запись до ответа клиенту (надёжнее, но медленнее и уязвима к отказу реплики); асинхронная — быстрее, но при падении лидера возможна **потеря последних записей** и чтение устаревших данных с фолловера (**replication lag**).`,
    },
    {
      id: 'sd-sharding',
      difficulty: 'hard',
      tags: ['databases', 'scalability'],
      related: ['sd-consistent-hashing', 'sd-replication', 'sd-sql-vs-nosql'],
      question: 'Что такое шардирование (партиционирование) и какие есть стратегии?',
      answer: `**Шардирование** — горизонтальное разбиение данных на части (**шарды**), каждая на своём узле. В отличие от репликации (копии одних данных), шарды хранят **разные** данные — это масштабирует и запись, и объём.

**Стратегии выбора шарда по ключу:**

- **Range-based** — по диапазонам ключа (A–M, N–Z). Удобно для диапазонных запросов, но легко получить **горячие шарды** при неравномерном распределении.
- **Hash-based** — по хешу ключа. Равномерно распределяет нагрузку, но диапазонные запросы становятся дорогими; при добавлении узла обычная хеш-функция требует **пересчёта почти всех ключей** (лечится консистентным хешированием).
- **Directory-based** — отдельный сервис-справочник хранит «ключ → шард». Гибко, но справочник — точка отказа.

**Проблемы:**

- **горячие ключи/шарды** (напр. очень популярный пользователь) — борются суффиксами ключа, репликацией горячих данных;
- **кросс-шардовые запросы и JOIN** становятся дорогими; распределённые транзакции сложны;
- **ребалансировка** при добавлении узлов.

Хороший **ключ шардирования** — с высокой кардинальностью и равномерным доступом.`,
    },
    {
      id: 'sd-consistent-hashing',
      difficulty: 'hard',
      tags: ['databases', 'scalability'],
      related: ['sd-sharding', 'sd-load-balancing'],
      question: 'Что такое консистентное хеширование и какую проблему оно решает?',
      answer: `Проблема обычного \`hash(key) % N\`: при изменении числа узлов **N** почти все ключи меняют владельца — массовая ребалансировка и промахи кэша.

**Консистентное хеширование** размещает и узлы, и ключи на одном **кольце** значений хеша (0…2³²). Ключ принадлежит **первому узлу по часовой стрелке**. Тогда при добавлении/удалении узла «переезжает» лишь ~**1/N** ключей (соседний участок кольца), а не почти все.

**Виртуальные узлы (vnodes):** каждый физический узел представляют многими точками на кольце. Это выравнивает распределение (иначе участки получаются неравномерными) и позволяет учитывать разную мощность узлов (более мощному — больше vnode).

**Где применяется:** распределённые кэши (memcached-кластеры), хранилища Dynamo-стиля (Cassandra, DynamoDB), шардирование, некоторые балансировщики. Это стандартный ответ на вопрос «как добавлять/убирать узлы без тотальной ребалансировки».`,
    },
    {
      id: 'sd-message-queue',
      difficulty: 'medium',
      tags: ['async', 'patterns'],
      related: ['sd-idempotency', 'sd-rate-limiting'],
      question: 'Зачем нужны очереди сообщений и асинхронная обработка?',
      answer: `**Очередь сообщений** (RabbitMQ, Kafka, SQS) — посредник, через который сервисы обмениваются сообщениями асинхронно: продюсер кладёт сообщение, консьюмер забирает, когда готов.

**Зачем:**

- **Разделение (decoupling)** — продюсер и консьюмер не знают друг о друге и не обязаны быть доступны одновременно;
- **Сглаживание пиков (buffering/backpressure)** — очередь принимает всплеск, консьюмеры разбирают в своём темпе, защищая БД от перегрузки;
- **Асинхронность** — тяжёлую работу (рассылка писем, обработка видео) выносят из синхронного запроса, ускоряя ответ пользователю;
- **Масштабирование** — несколько консьюмеров разбирают очередь параллельно;
- **Надёжность** — при падении консьюмера сообщение не теряется, обработается позже.

**Важные детали:** гарантии доставки (**at-most-once / at-least-once / exactly-once**; чаще at-least-once, поэтому обработчики должны быть **идемпотентны**), порядок сообщений (Kafka хранит порядок в пределах партиции), **dead-letter queue** для «ядовитых» сообщений, отслеживание отставания консьюмеров (lag).`,
    },
    {
      id: 'sd-rate-limiting',
      difficulty: 'medium',
      tags: ['reliability', 'api'],
      related: ['sd-idempotency', 'sd-api-design'],
      question: 'Что такое rate limiting и какие есть алгоритмы?',
      answer: `**Rate limiting** ограничивает число запросов от клиента за интервал — защита от злоупотреблений, DDoS, «шумных соседей» и перегрузки. Превышение → ответ **429 Too Many Requests**.

**Алгоритмы:**

- **Token Bucket** — в «ведро» с ёмкостью capacity с постоянной скоростью капают токены; каждый запрос тратит токен, нет токенов → отказ. Допускает короткие всплески. Самый популярный.
- **Leaky Bucket** — запросы попадают в очередь и «вытекают» с фиксированной скоростью; сглаживает трафик до равномерного, всплески обрезает.
- **Fixed Window** — счётчик на фиксированное окно (напр. 100 запросов/минуту). Прост, но даёт всплеск в **2×** на стыке окон.
- **Sliding Window (log/counter)** — учитывает скользящее окно, убирая проблему границы; точнее, но дороже по памяти.

**Практика:** лимит держат в быстром общем хранилище (**Redis**) — чтобы он работал согласованно на всех инстансах; идентифицируют клиента по API-ключу/пользователю/IP; возвращают заголовки \`Retry-After\` и \`X-RateLimit-*\`.`,
    },
    {
      id: 'sd-idempotency',
      difficulty: 'medium',
      tags: ['reliability', 'api'],
      related: ['sd-message-queue', 'sd-rate-limiting'],
      question: 'Что такое идемпотентность и почему она важна в распределённых системах?',
      answer: `**Идемпотентная операция** — повторное выполнение с теми же параметрами даёт **тот же результат**, что и однократное, без дополнительных побочных эффектов.

Почему важно: в распределённых системах повторы **неизбежны** — таймауты, ретраи клиента, доставка «at-least-once» из очередей. Если «списать деньги» не идемпотентно, повтор спишет дважды.

**Как обеспечивают:**

- **Idempotency key** — клиент присылает уникальный ключ операции; сервер запоминает обработанные ключи и на повтор возвращает прежний результат, не выполняя действие снова;
- **Natural idempotency** — операция сама по себе идемпотентна: \`SET x = 5\` (в отличие от \`x = x + 1\`), \`PUT\` (замена ресурса целиком).

**HTTP-семантика:** \`GET\`, \`PUT\`, \`DELETE\` считаются идемпотентными; \`POST\` — **нет** (поэтому для платежей/заказов добавляют idempotency key). Идемпотентность — обязательный спутник ретраев и «at-least-once»-обработки.`,
    },
    {
      id: 'sd-api-design',
      difficulty: 'medium',
      tags: ['api'],
      related: ['sd-rate-limiting', 'sd-idempotency'],
      question: 'Какие принципы важны при проектировании REST API?',
      answer: `**REST** строит API вокруг **ресурсов** (существительные, не глаголы) и HTTP-методов.

- **Ресурсы и методы:** \`GET /users/42\` (получить), \`POST /users\` (создать), \`PUT/PATCH /users/42\` (обновить), \`DELETE /users/42\`. Глаголы — в методе, а не в URL (\`/getUser\` — плохо).
- **Статус-коды:** 2xx успех, 4xx ошибка клиента (400, 401, 403, 404, 429), 5xx ошибка сервера. Используйте их осмысленно.
- **Пагинация** — не отдавайте огромные списки целиком. **Offset-based** (\`?page=3&size=20\`) проста, но «плывёт» при вставках и дорога на больших смещениях; **cursor-based** (\`?after=<id>\`) стабильна и эффективна для лент.
- **Версионирование** — \`/v1/...\` или через заголовок; позволяет менять контракт, не ломая клиентов.
- **Идемпотентность и безопасность методов** — \`GET\` не меняет состояние; для небезопасных ретраев — idempotency key.
- **Прочее:** консистентные имена (мн. число, kebab/snake), фильтрация/сортировка через query-параметры, единый формат ошибок, rate limiting, аутентификация/авторизация, HATEOAS (реже).

Альтернативы для контекста: **gRPC** (быстрый бинарный RPC для внутренних сервисов), **GraphQL** (клиент сам выбирает поля, гибкие запросы).`,
    },
    {
      id: 'sd-microservices-monolith',
      difficulty: 'medium',
      tags: ['patterns', 'scalability'],
      related: ['sd-message-queue', 'sd-observability'],
      question: 'Микросервисы или монолит — как выбрать?',
      answer: `**Монолит** — всё приложение как один разворачиваемый модуль.

- ➕ проще разрабатывать, тестировать, деплоить на старте; транзакции и вызовы — локальные; нет сетевых задержек между модулями;
- ➖ с ростом кодовой базы и команды: медленный деплой, сложно масштабировать части независимо, одна ошибка может уронить всё.

**Микросервисы** — набор небольших независимых сервисов, каждый со своей БД, общаются по сети (REST/gRPC/очереди).

- ➕ независимые деплой и масштабирование, свобода технологий, изоляция отказов, соответствие структуре команд;
- ➖ **распределённая сложность**: сетевые сбои, задержки, распределённые транзакции (saga), консистентность данных, нужны сервис-дискавери, трейсинг, оркестрация, DevOps-зрелость.

**Практический совет:** начинать почти всегда стоит с **(модульного) монолита** и выделять сервисы, когда появляются реальные причины — узкие места масштабирования, независимые релизы, размер команды. Микросервисы решают **организационные и масштабные** проблемы ценой инфраструктурной сложности; преждевременное дробление — частая ошибка.`,
    },
    {
      id: 'sd-observability',
      difficulty: 'easy',
      tags: ['reliability'],
      related: ['sd-load-balancing', 'sd-microservices-monolith'],
      question: 'Что такое наблюдаемость (observability) и три её столпа?',
      answer: `**Наблюдаемость** — способность понять внутреннее состояние системы по её внешним сигналам, особенно чтобы диагностировать проблемы, которые не предвидели заранее.

**Три столпа:**

- **Метрики (metrics)** — числовые ряды во времени: QPS, задержки (p50/p95/p99), доля ошибок, использование CPU/памяти, длина очередей. Дёшевы, годятся для дашбордов и **алертов**.
- **Логи (logs)** — дискретные записи о событиях. Ценны для детального разбора; в распределённой системе важны **структурированные** логи и общий **correlation/trace id**, чтобы связать записи одного запроса.
- **Трейсы (traces)** — путь одного запроса через все сервисы со временем на каждом шаге. Показывают, **где** именно теряется время и какой сервис — узкое место (distributed tracing, напр. OpenTelemetry).

Дополнительно: **алертинг** по метрикам, **SLI/SLO/SLA** (что измеряем, целевой уровень, обязательства), health checks. Хорошая наблюдаемость — то, что превращает «всё лежит, непонятно почему» в конкретную причину за минуты.`,
    },
    {
      id: 'sd-url-shortener',
      difficulty: 'hard',
      tags: ['design-example'],
      related: ['sd-approach', 'sd-sharding', 'sd-caching', 'sd-consistent-hashing'],
      question: 'Спроектируйте сокращатель ссылок (например, bit.ly).',
      answer: `Классический end-to-end пример, показывающий весь процесс.

**1. Требования.** Функциональные: создать короткий код по длинному URL; редирект по короткому коду. Нефункциональные: очень высокое соотношение **чтений к записям** (~100:1), низкая задержка редиректа, высокая доступность, коды уникальны и не угадываются.

**2. Оценка.** Пусть 100 млн новых ссылок/мес ≈ ~40 записей/с; чтений ~×100 ≈ ~4000/с. За 5 лет ~6 млрд ссылок → нужен короткий код: при алфавите base62 (a–z,A–Z,0–9) **7 символов = 62⁷ ≈ 3.5 трлн** комбинаций — с запасом.

**3. API.** \`POST /shorten {longUrl}\` → \`{shortUrl}\`; \`GET /{code}\` → **301/302** редирект.

**4. Генерация кода.**
- **Счётчик + base62** — глобальный автоинкремент, закодированный в base62. Уникально и коротко; счётчик масштабируют диапазонами (напр. через Zookeeper/распределённый генератор ID), чтобы не было единой точки.
- Альтернатива — **хеш** от URL (взять первые N символов base62) с разрешением коллизий.

**5. Хранение.** Таблица \`code → longUrl\` (+ метаданные). Данных много, паттерн доступа — простой доступ по ключу → хорошо ложится на **key-value/NoSQL** с **шардированием по code** (и консистентным хешированием при добавлении узлов).

**6. Чтение (горячий путь).** Редирект должен быть очень быстрым: перед БД ставим **кэш** (Redis) на популярные коды (правило 80/20). Промах → БД → кладём в кэш. Перед всем — CDN/edge для геораспределения.

**7. Масштаб и надёжность.** Stateless-сервисы за балансировщиком, репликация БД (read-реплики под чтения), rate limiting на создание, аналитика кликов — асинхронно через **очередь**, чтобы не тормозить редирект. 301 vs 302: 301 кэшируется браузером (быстрее, но хуже для аналитики) — обычно берут **302**.`,
    },
  ],
};
