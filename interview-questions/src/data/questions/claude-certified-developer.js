// Категория: claude-certified-developer
// Оригинальный учебный раздел по публичным темам экзамена Claude Certified
// Developer (Foundations): основы работы с Claude API. Факты актуальны на момент
// подготовки — конкретные ID моделей и цены со временем меняются, сверяйтесь с
// официальной документацией Anthropic.
import { ccdvImportedQuestions } from '../ccdvImported-questions.js';
import { ccdvStudyQuestions } from '../ccdvStudy-questions.js';

// Подтема (домен экзамена) для каждого вопроса — по ним группируется список на
// странице категории. 8 официальных доменов CCDV-F.
const SUBTOPIC = {
  // — наши оригинальные вопросы —
  'messages-api-architecture': 'Applications & Integration',
  'what-is-a-token': 'Model Selection & Optimisation',
  'context-window': 'Model Selection & Optimisation',
  'temperature-determinism': 'Model Selection & Optimisation',
  'model-selection': 'Model Selection & Optimisation',
  'thinking-and-effort': 'Model Selection & Optimisation',
  'prompt-structure': 'Prompt & Context Engineering',
  'structured-output': 'Prompt & Context Engineering',
  'prompt-caching': 'Model Selection & Optimisation',
  'batch-api': 'Model Selection & Optimisation',
  'tool-use-basics': 'Tools & MCPs',
  'tool-use-parallel-errors': 'Tools & MCPs',
  'server-vs-client-tools': 'Tools & MCPs',
  streaming: 'Applications & Integration',
  'stop-reasons-refusal': 'Applications & Integration',
  'sdk-and-auth': 'Applications & Integration',
  'streaming-events': 'Applications & Integration',
  'vision-images': 'Applications & Integration',
  'pdf-documents': 'Applications & Integration',
  'multi-turn-stateless': 'Applications & Integration',
  'rate-limits-and-errors': 'Applications & Integration',
  'tool-runner-vs-manual': 'Tools & MCPs',
  'structured-output-limits': 'Prompt & Context Engineering',
  'models-api': 'Model Selection & Optimisation',
  'prompt-caching-placement': 'Model Selection & Optimisation',
  'cache-invalidation-levels': 'Model Selection & Optimisation',
  'stop-sequences': 'Applications & Integration',
  'context-editing-vs-compaction': 'Prompt & Context Engineering',
  'workflows-vs-agents': 'Agents & Workflows',
  'workflow-patterns': 'Agents & Workflows',
  'agent-when-and-risks': 'Agents & Workflows',
  'tool-design-aci': 'Agents & Workflows',
  'what-is-claude-code': 'Claude Code',
  'claude-md-memory': 'Claude Code',
  'claude-code-config': 'Claude Code',
  'why-evals': 'Eval, Testing & Debugging',
  'eval-grading-methods': 'Eval, Testing & Debugging',
  'debugging-prompts': 'Eval, Testing & Debugging',
  'prompt-injection': 'Security & Safety',
  guardrails: 'Security & Safety',
  'responsible-use': 'Security & Safety',
  // — импортированные multi-response вопросы (ccd-qa-*) —
  'ccd-qa-1': 'Prompt & Context Engineering',
  'ccd-qa-2': 'Prompt & Context Engineering',
  'ccd-qa-3': 'Claude Code',
  'ccd-qa-4': 'Security & Safety',
  'ccd-qa-5': 'Eval, Testing & Debugging',
  'ccd-qa-6': 'Applications & Integration',
  'ccd-qa-7': 'Applications & Integration',
  'ccd-qa-8': 'Prompt & Context Engineering',
  'ccd-qa-9': 'Security & Safety',
  'ccd-qa-10': 'Security & Safety',
  'ccd-qa-11': 'Eval, Testing & Debugging',
  'ccd-qa-12': 'Applications & Integration',
  'ccd-qa-13': 'Applications & Integration',
  'ccd-qa-14': 'Prompt & Context Engineering',
  'ccd-qa-15': 'Security & Safety',
  'ccd-qa-16': 'Security & Safety',
  'ccd-qa-17': 'Applications & Integration',
  'ccd-qa-18': 'Applications & Integration',
  'ccd-qa-19': 'Applications & Integration',
  'ccd-qa-20': 'Claude Code',
  'ccd-qa-21': 'Security & Safety',
  'ccd-qa-22': 'Security & Safety',
  'ccd-qa-23': 'Applications & Integration',
  'ccd-qa-24': 'Agents & Workflows',
  'ccd-qa-25': 'Prompt & Context Engineering',
  'ccd-qa-26': 'Applications & Integration',
  'ccd-qa-27': 'Security & Safety',
};

export const claudeCertifiedDeveloper = {
  id: 'claude-certified-developer',
  title: 'Claude Certified Developer',
  icon: '🎓',
  description: 'Подготовка к экзамену: основы Claude API — модели, промптинг, инструменты, токены',
  intro: `📚 **Официальные материалы Anthropic для подготовки к экзамену Claude Certified Developer: Foundations (CCDV‑F):**

- Обзор сертификаций Anthropic (включая Developer): [claude.com/blog/four-role-based-claude-certifications](https://claude.com/blog/four-role-based-claude-certifications)
- **Anthropic Academy** — курсы и официальный exam guide (blueprint с доменами и весами): [anthropic.skilljar.com](https://anthropic.skilljar.com)
- Учебные ресурсы: [anthropic.com/learn](https://www.anthropic.com/learn) · [claude.com/resources/courses](https://claude.com/resources/courses)
- Регистрация и сдача (Pearson VUE): [pearsonvue.com/us/en/anthropic.html](https://www.pearsonvue.com/us/en/anthropic.html)

**Факты об экзамене** (Anthropic Exam Guide v1.0, июль 2026): код **CCDV‑F** · **53** вопроса (single/multiple‑response, в каждом указано, сколько вариантов выбрать) · **120** минут · проходной балл **720** по шкале 100–1000 · стоимость **$125** за попытку · действителен **12 месяцев** · проведение через **Pearson VUE** (онлайн с прокторингом или в тест‑центре) · пересдачи через 14/30/90 дней, до 4 попыток в год · в отчёте — pass/fail, балл и **% верных по доменам**.

**Blueprint — веса доменов** (официальные; по ним мы соразмеряем материал):

| Домен | Вес |
|---|---|
| Applications & Integration | 33.1% |
| Model Selection & Optimisation | 16.8% |
| Agents & Workflows | 14.7% |
| Prompt & Context Engineering | 11% |
| Tools & MCPs | 10.6% |
| Security & Safety | 8.1% |
| Claude Code | 3.1% |
| Eval, Testing & Debugging | 2.6% |

Раздел ниже — неофициальный тренажёр по этим темам, включая практический набор из ~176 сценарных вопросов, адаптированный из публичного учебного артефакта (автор **Anas Riad**). Стоимость экзамена — это плата Anthropic за официальный экзамен, здесь ничего платить не нужно.`,
  questions: [
    {
      id: 'messages-api-architecture',
      question: 'Через какой эндпоинт идут запросы к Claude и как в него встроены инструменты и структурированный вывод?',
      answer: `Практически всё в Claude API проходит через единственный эндпоинт — **\`POST /v1/messages\`** (Messages API). Это не «чат-эндпоинт плюс отдельные API для инструментов»: **tool use и ограничение формата ответа — это возможности того же самого эндпоинта**, а не отдельные сервисы.

Запрос как минимум содержит:

- **\`model\`** — идентификатор модели (например, \`claude-opus-5\`);
- **\`max_tokens\`** — жёсткий потолок на длину ответа;
- **\`messages\`** — история диалога: массив \`{ role, content }\`, роли чередуются \`user\`/\`assistant\`, первый элемент — всегда \`user\`.

Поверх этого — необязательные поля: \`system\` (системный промпт), \`tools\` (инструменты), \`output_config\` (в т.ч. \`format\` для структурированного вывода и \`effort\`), \`thinking\`, \`stream\`.

Вспомогательные эндпоинты (Batches, Files, Token Counting, Models) **питают** запросы к Messages API, но сама генерация — всегда через \`/v1/messages\`. API **stateless**: состояния диалога на сервере нет, всю историю вы отправляете в каждом запросе заново.`,
    },
    {
      id: 'what-is-a-token',
      question: 'Что такое токен и почему нельзя оценивать длину промпта библиотекой tiktoken?',
      answer: `**Токен** — минимальная единица, которой модель оперирует с текстом: обычно это часть слова (subword), целое короткое слово, знак препинания или пробел. Тарификация и лимиты (контекстное окно, \`max_tokens\`, rate limits) считаются **в токенах**, а не в символах или словах.

Важное следствие для разработчика: **токенизация зависит от модели**. Один и тот же текст даёт разное число токенов на разных семействах моделей.

Оценивать длину через **\`tiktoken\` нельзя** — это токенизатор OpenAI. Он занижает счёт для Claude примерно на 15–20% на обычном тексте и сильнее — на коде и не-английском. Точное число даёт эндпоинт **\`POST /v1/messages/count_tokens\`** (в SDK — \`client.messages.count_tokens(...)\`), которому передают ту же модель, что и для генерации:

\`\`\`python
resp = client.messages.count_tokens(
    model="claude-opus-5",
    messages=[{"role": "user", "content": open("doc.md").read()}],
)
print(resp.input_tokens)
\`\`\`

Эндпоинт stateless — чтобы посчитать «дельту» между двумя версиями текста, считают каждую отдельно и вычитают.`,
    },
    {
      id: 'context-window',
      question: 'Что такое контекстное окно и чем stop_reason "model_context_window_exceeded" отличается от "max_tokens"?',
      answer: `**Контекстное окно** — максимальное число токенов, которое модель может держать «в голове» за один запрос: это **сумма** входа (система + история + текущее сообщение) и генерируемого выхода. У актуальных моделей (Opus 5, Sonnet 5, Fable 5) окно — **до 1M токенов**; у Haiku 4.5 — **200K**. Точные значения лучше смотреть через Models API (\`max_input_tokens\`).

Не путайте два потолка:

- **\`max_tokens\`** — заданный вами лимит на **длину ответа**. Если он достигнут, приходит \`stop_reason: "max_tokens"\` — ответ обрезан, нужно поднять лимит или включить стриминг.
- **Контекстное окно** — общий лимит модели. Если он исчерпан, приходит \`stop_reason: "model_context_window_exceeded"\` — историю нужно **сжать** (compaction) или разбить.

Оба надо обрабатывать отдельно:

\`\`\`python
if response.stop_reason == "model_context_window_exceeded":
    # окно исчерпано — сжать или разделить диалог
    ...
elif response.stop_reason == "max_tokens":
    # упёрлись в потолок ответа — увеличить max_tokens / стримить
    ...
\`\`\``,
    },
    {
      id: 'temperature-determinism',
      question: 'Гарантирует ли temperature 0 одинаковый ответ на одинаковый промпт?',
      answer: `Нет. **Детерминизм не гарантируется** даже при \`temperature = 0\`. Низкая температура лишь смещает выбор к наиболее вероятным токенам, но **не даёт жёсткой гарантии** побайтово одинакового вывода — на инфраструктуре модели остаётся источник вариативности. Поэтому нельзя строить логику на «при 0 всегда одно и то же».

Что при этом **не** объясняет расхождение: включённый стриминг, размер контекстного окна — это отдельные вещи, не влияющие на формулировку.

Практический нюанс актуальных моделей: у Opus 5 / Sonnet 5 / Fable 5 **параметры сэмплирования \`temperature\`, \`top_p\`, \`top_k\` вообще убраны** — их передача возвращает \`400\`. Поведение теперь настраивают **промптингом** и параметром \`effort\`, а не температурой. Если вам раньше нужна была «стабильность», добивайтесь её чётким промптом и низким \`effort\`; если нужна была «вариативность» — просите её явно в тексте промпта.`,
    },
    {
      id: 'model-selection',
      question: 'Как выбрать модель Claude под задачу?',
      answer: `Выбор — это баланс **интеллект ↔ скорость ↔ стоимость**. Ориентиры по актуальным семействам (цены — за 1M токенов вход/выход, на момент подготовки):

| Модель | ID | Когда брать | ~Цена |
|---|---|---|---|
| Fable 5 | \`claude-fable-5\` | Самые сложные, долгие агентные задачи | $10 / $50 |
| Opus 5 | \`claude-opus-5\` | Сложный код, агенты, глубокие рассуждения | $5 / $25 |
| Sonnet 5 | \`claude-sonnet-5\` | Баланс скорости и качества — большинство задач | $3 / $15 |
| Haiku 4.5 | \`claude-haiku-4-5\` | Простые задачи, максимальная скорость и дешевизна | $1 / $5 |

Практические правила:

- по умолчанию берите **последнюю Opus** для своего уровня, если явно не выбрали иное — «понижать ради экономии» стоит осознанно;
- используйте **точные строки-ID** без дописывания дат-суффиксов;
- для «живых» характеристик (окно, поддержка возможностей) есть **Models API** (\`client.models.retrieve(id)\`), а не догадки.

Не подбирайте модель по названию из головы — сверяйтесь со списком актуальных ID в документации, он меняется.`,
    },
    {
      id: 'thinking-and-effort',
      question: 'Что такое adaptive thinking и параметр effort? Что стало с budget_tokens?',
      answer: `**Adaptive thinking** — режим, в котором модель **сама решает**, когда и насколько глубоко «подумать» перед ответом, в том числе между вызовами инструментов. Включается через \`thinking: {type: "adaptive"}\`.

**\`effort\`** (внутри \`output_config\`) — основной рычаг «тщательность ↔ расход токенов»: уровни **\`low\` / \`medium\` / \`high\` / \`xhigh\` / \`max\`**. Ниже effort → меньше и более собранные шаги, короче преамбулы; выше → тщательнее проверка и рассуждения. Значение по умолчанию — \`high\`.

Фиксированный «бюджет размышлений» **\`budget_tokens\` устарел**: на Opus 5 / Sonnet 5 / Fable 5 его передача возвращает \`400\`. Его концептуально заменил \`effort\`.

\`\`\`python
client.messages.create(
    model="claude-opus-5",
    max_tokens=16000,
    thinking={"type": "adaptive"},
    output_config={"effort": "high"},  # low | medium | high | xhigh | max
    messages=[...],
)
\`\`\`

Нюанс: на Opus 5 мышление включено **по умолчанию** (пропуск поля \`thinking\` = adaptive), а «сырой» ход рассуждений наружу не отдаётся — можно получить лишь краткое summary (\`display: "summarized"\`).`,
    },
    {
      id: 'prompt-structure',
      question: 'Как структурировать промпт для Claude: система, роль, примеры?',
      answer: `Хорошая структура промпта = предсказуемый результат. Основные приёмы:

- **Системный промпт (\`system\`)** задаёт роль, правила и формат: «ты — ассистент, отвечающий кратко и по-русски». Держите его **стабильным** (это важно для кэширования, см. prompt caching).
- **Few-shot примеры** — один-два образца «вход → желаемый выход» в истории \`messages\` заметно поднимают качество на нетривиальных задачах. Zero-shot (без примеров) годится для простого и хорошо описанного.
- **Структурируйте длинный контекст** — разделяйте инструкции, данные и вопрос явными секциями (часто с XML-подобными тегами вроде \`<document>...</document>\`), чтобы модель не путала, где что.
- **Не переусердствуйте с «CRITICAL / YOU MUST»** — современные модели следуют инструкциям буквально, и агрессивные формулировки заставляют инструмент/поведение срабатывать слишком часто. Пишите «Используй X, когда…», а не «ВСЕГДА используй X».

Ключевая мысль: описывайте **что** нужно и **когда**, приводите примеры желаемого — это работает лучше, чем длинные списки запретов.`,
    },
    {
      id: 'structured-output',
      question: 'Как гарантированно получить от Claude валидный JSON заданной схемы?',
      answer: `Правильный способ — **структурированный вывод** через \`output_config.format\` с JSON-схемой (или строгие инструменты со \`strict: true\`), а **не** «префилл» ответа ассистента.

\`\`\`python
response = client.messages.create(
    model="claude-opus-5",
    max_tokens=1024,
    output_config={"format": {"type": "json_schema", "schema": SCHEMA}},
    messages=[{"role": "user", "content": "Извлеки имя и email."}],
)
\`\`\`

Важные моменты:

- **Префилл больше не работает**: заканчивать \`messages\` сообщением \`role: "assistant"\` с началом JSON на актуальных моделях (Opus 5, Sonnet 5, Fable 5, семейство 4.6+) **возвращает 400**. Раньше так «форсировали» формат — теперь для этого есть structured outputs.
- Для фиксированной метки-классификации удобно объявить инструмент с полем-\`enum\`.
- Если пришёл \`stop_reason: "refusal"\` или \`"max_tokens"\`, вывод может не соответствовать схеме — это надо проверять.
- Первая генерация с новой схемой чуть медленнее (компиляция схемы), дальше — кэш.

SDK-хелпер \`client.messages.parse()\` дополнительно валидирует ответ по схеме автоматически.`,
    },
    {
      id: 'prompt-caching',
      question: 'Как работает prompt caching и в чём его главное ограничение?',
      answer: `**Prompt caching — это префиксное совпадение.** Кэшируется общий **префикс** запроса, и **любое изменение байта в префиксе аннулирует кэш для всего, что идёт после**. Порядок сборки промпта: \`tools\` → \`system\` → \`messages\`.

Из этого следует главное правило: **стабильное — в начало, изменчивое — в конец**. Замороженный системный промпт и детерминированный список инструментов — впереди; таймстемпы, per-request ID, меняющийся вопрос — после последней точки кэширования.

- Метка ставится через \`cache_control: {type: "ephemeral"}\` на блоке; TTL по умолчанию **5 минут**, можно \`"1h"\`.
- Минимальный кэшируемый префикс зависит от модели (например, 512 токенов на Opus 5) — короче просто не закэшируется, без ошибки.
- **Проверяйте попадания** по \`usage.cache_read_input_tokens\`: если он стабильно 0 при одинаковом префиксе — где-то «тихий инвалидатор»: \`datetime.now()\` в системном промпте, несортированный JSON, меняющийся набор инструментов.

Экономика: чтение из кэша ≈ 0.1× цены входа, запись ≈ 1.25× (5 мин) или 2× (1 час) — окупается со второго-третьего запроса.`,
    },
    {
      id: 'batch-api',
      question: 'Как дешевле обработать большой объём запросов, если ответ не нужен мгновенно?',
      answer: `Для **неспешной** массовой обработки есть **Message Batches API** (\`POST /v1/messages/batches\`): запросы выполняются **асинхронно** со скидкой **50%** от обычной цены токенов.

- В батч можно положить **до 100 000** запросов (или 256 МБ).
- Большинство батчей завершается за час, максимум — **24 часа**; результаты доступны 29 дней.
- Поддерживаются все возможности Messages API (инструменты, vision, кэширование).

\`\`\`python
batch = client.messages.batches.create(requests=[
    {"custom_id": "q1", "params": {"model": "claude-opus-5", "max_tokens": 1024,
        "messages": [{"role": "user", "content": "..."}]}},
    # ...
])
# опрашиваем, пока processing_status != "ended", затем читаем результаты
\`\`\`

Ключевой нюанс: результаты приходят **в произвольном порядке** — сопоставляйте их по \`custom_id\`, а не по позиции. Batch подходит для оффлайн-задач (разметка, извлечение, эмбеддинги), но не для интерактивного ответа пользователю.`,
    },
    {
      id: 'tool-use-basics',
      question: 'Как устроен цикл tool use (function calling) в Messages API?',
      answer: `**Tool use** позволяет модели вызывать ваши функции. Вы описываете инструменты, модель просит их вызвать, вы исполняете и возвращаете результат — и так по кругу.

1. Передаёте \`tools\` — каждый инструмент это \`{ name, description, input_schema }\`. **Описание важнее всего**: модель по нему решает, когда звать инструмент (пишите «вызывай, когда…»).
2. Если модель хочет вызвать инструмент, ответ приходит со \`stop_reason: "tool_use"\` и блоком \`tool_use\` (\`id\`, \`name\`, \`input\`).
3. Вы **исполняете** функцию у себя и добавляете в диалог сообщение \`role: "user"\` с блоком \`tool_result\`, где **\`tool_use_id\`** совпадает с id вызова.
4. Повторяете, пока не придёт \`stop_reason: "end_turn"\`.

\`\`\`json
{ "type": "tool_result", "tool_use_id": "toolu_123",
  "content": "72°F, ясно" }
\`\`\`

Управлять поведением помогает \`tool_choice\`: \`auto\` (по умолчанию), \`any\` (обязан вызвать хоть что-то), \`{type:"tool", name}\` (конкретный), \`none\`. В SDK есть **tool runner**, который крутит этот цикл за вас; ручной цикл нужен, когда хотите полностью владеть логикой.`,
    },
    {
      id: 'tool-use-parallel-errors',
      question: 'Как обрабатывать несколько tool_use в одном ответе и ошибки инструментов?',
      answer: `Модель может запросить **несколько инструментов сразу** — в одном ответе будет несколько блоков \`tool_use\`. Правильная обработка:

- **Исполните их все** (можно параллельно) и верните **все** \`tool_result\` **одним** сообщением \`role: "user"\`. Если разбить результаты на несколько сообщений, вы «приучите» модель перестать делать параллельные вызовы.
- Порядок: сначала добавляете в историю ответ ассистента целиком (\`response.content\` — со всеми \`tool_use\`), затем одно пользовательское сообщение со всеми результатами.

Ошибки инструментов **нельзя молча терять**. Если функция упала, всё равно верните для неё \`tool_result\`, но с флагом **\`is_error: true\`** и текстом ошибки:

\`\`\`json
{ "type": "tool_result", "tool_use_id": "toolu_9",
  "content": "Сервис недоступен (503)", "is_error": true }
\`\`\`

Модель увидит ошибку и сможет подстроиться — попробовать иначе или уточнить. Пропущенный результат для запрошенного \`tool_use\` сломает диалог.`,
    },
    {
      id: 'server-vs-client-tools',
      question: 'Чем серверные инструменты отличаются от клиентских (пользовательских)?',
      answer: `Инструменты бывают двух видов — разница в том, **кто их исполняет**.

**Клиентские (пользовательские) инструменты** исполняете **вы**: модель возвращает \`tool_use\`, вы у себя запускаете код и присылаете \`tool_result\`. Это ваши функции — доступ к БД, вызов вашего API, отправка письма. Клиентскими являются и «схемные» инструменты Anthropic вроде bash и text editor: схему задаёт Anthropic, но выполняете команды вы.

**Серверные инструменты** исполняются **на инфраструктуре Anthropic** — отдельного клиентского цикла нет. Вы лишь объявляете их в \`tools\`, а результат приходит блоками в том же ответе. Примеры:

- **web search / web fetch** — поиск и загрузка веб-страниц (для информации свежее обучающих данных);
- **code execution** — запуск кода в песочнице на стороне Anthropic.

Практический нюанс для серверных инструментов: серверный цикл может дойти до лимита итераций и вернуть \`stop_reason: "pause_turn"\` — тогда нужно просто повторно отправить сообщение с ответом ассистента, и генерация продолжится с того же места (никакого «Continue» дописывать не надо).`,
    },
    {
      id: 'streaming',
      question: 'Когда и зачем использовать стриминг ответов?',
      answer: `**Стриминг** отдаёт ответ по мере генерации через Server-Sent Events, а не одним куском в конце.

Две причины его использовать:

1. **UX** — в чат-интерфейсах текст появляется сразу, а не после паузы.
2. **Надёжность (главное для бэкенда)** — длинные входы/выходы и большой \`max_tokens\` при **не**стримовом запросе рискуют упереться в **HTTP-таймаут** SDK. Правило: если \`max_tokens\` больше ~16 000 — **стримьте**. У всех актуальных моделей выход до 128K достижим только со стримингом.

\`\`\`python
with client.messages.stream(
    model="claude-opus-5", max_tokens=64000,
    messages=[{"role": "user", "content": "..."}],
) as stream:
    for text in stream.text_stream:
        print(text, end="")
    final = stream.get_final_message()  # полный объект ответа
\`\`\`

Если отдельные события обрабатывать не нужно — используйте хелпер \`get_final_message()\` / \`finalMessage()\`, он соберёт полный ответ за вас. Отдельный нюанс: при стриминге размышлений (\`display: "summarized"\`) по умолчанию идёт долгая «пауза» — блоки thinking приходят пустыми, если summary не запрошено.`,
    },
    {
      id: 'stop-reasons-refusal',
      question: 'Какие бывают stop_reason и как правильно обрабатывать refusal?',
      answer: `Поле **\`stop_reason\`** в ответе объясняет, почему модель остановилась. Основные значения:

- **\`end_turn\`** — ответ завершён естественно;
- **\`max_tokens\`** — упёрлись в лимит длины ответа;
- **\`tool_use\`** — модель хочет вызвать инструмент;
- **\`pause_turn\`** — серверный инструмент достиг лимита итераций, диалог можно продолжить;
- **\`refusal\`** — модель отказалась по соображениям безопасности;
- **\`stop_sequence\`** — сработала заданная стоп-последовательность.

Ключевое правило: **проверяйте \`stop_reason\` до чтения \`content\`**. При отказе классификаторов ответ приходит с **HTTP 200** и \`stop_reason: "refusal"\`, а массив \`content\` может быть пустым — код, который безусловно читает \`content[0].text\`, на отказе сломается.

\`\`\`python
if response.stop_reason == "refusal":
    handle_refusal()      # content пуст или частичный
else:
    print(response.content[0].text)
\`\`\`

Детали отказа (когда есть) лежат в \`stop_details\` — но это поле информационное и может быть \`null\` даже при отказе, поэтому ветвитесь именно по \`stop_reason\`.`,
    },
    {
      id: 'sdk-and-auth',
      question: 'Как аутентифицироваться в Claude API и какой SDK использовать?',
      answer: `Стандартный способ — **переменная окружения \`ANTHROPIC_API_KEY\`**, которую SDK подхватывает автоматически: конструктор клиента без аргументов (\`Anthropic()\`) уже готов к работе. Альтернатива для локальной разработки — OAuth-профиль через \`ant auth login\` (SDK читает его с диска, отдельный ключ не нужен).

Ключ **нельзя** держать в коде или репозитории — только в переменных окружения или секрет-хранилище. Утёкший ключ — это скомпрометированный доступ ко всему аккаунту.

Для вызовов используйте **официальный SDK своего языка** (\`anthropic\` для Python, \`@anthropic-ai/sdk\` для TypeScript и т.д.), а не самописный HTTP-клиент, если только вам явно не нужен raw HTTP. SDK берёт на себя ретраи, типизированные ошибки, стриминг и хелперы для инструментов.

\`\`\`python
from anthropic import Anthropic
client = Anthropic()   # ключ из ANTHROPIC_API_KEY
\`\`\`

При \`401\` первым делом проверьте, что ключ задан и не пуст (пустой \`ANTHROPIC_API_KEY=""\` тоже «выигрывает» слот и уходит на сервер пустым).`,
    },
    {
      id: 'streaming-events',
      question: 'Из каких событий состоит стриминг ответа (SSE)?',
      answer: `При стриминге ответ приходит потоком **Server-Sent Events**. Основные типы событий:

- **\`message_start\`** — метаданные сообщения (один раз в начале);
- **\`content_block_start\`** — начался блок контента (текст, thinking, tool_use);
- **\`content_block_delta\`** — очередная порция (\`text_delta\`, \`thinking_delta\`, \`input_json_delta\`);
- **\`content_block_stop\`** — блок завершён;
- **\`message_delta\`** — обновления уровня сообщения: здесь приходит \`stop_reason\` и \`usage\`;
- **\`message_stop\`** — сообщение завершено.

Практика: если нужен только итог — не разбирайте события руками, возьмите хелпер \`get_final_message()\` / \`finalMessage()\`, он соберёт полный объект. Для чат-интерфейса печатайте \`text_delta\` по мере поступления.

\`\`\`python
with client.messages.stream(model="claude-opus-5", max_tokens=1024,
        messages=[{"role": "user", "content": "Привет"}]) as stream:
    for text in stream.text_stream:
        print(text, end="")
\`\`\`

Нюанс: при \`display: "summarized"\` события thinking приходят пустыми, если summary не запрошено, — визуально это выглядит как пауза перед ответом.`,
    },
    {
      id: 'vision-images',
      question: 'Как передавать изображения модели (vision) и какие есть ограничения?',
      answer: `Изображение передают **блоком контента** \`type: "image"\` в сообщении пользователя, а его источник — по **URL** или **base64**:

\`\`\`python
{"type": "image", "source": {"type": "base64",
    "media_type": "image/png", "data": b64_string}}
\`\`\`

- Поддерживаются JPEG, PNG, GIF, WebP.
- Блок изображения обычно ставят **перед** текстовым блоком с вопросом.
- Изображения стоят токенов — большие картинки дороже; если высокое разрешение не нужно, уменьшайте их на своей стороне.
- Точное число токенов на конкретное изображение можно узнать через \`count_tokens\`.

Vision полезен для разбора скриншотов, диаграмм, документов и UI. Как и с текстом, **не обрезайте** молча вход — если он не помещается в окно, обсудите с пользователем чанкинг, а не тихую потерю данных.`,
    },
    {
      id: 'pdf-documents',
      question: 'Как дать модели PDF-документ и зачем нужен Files API?',
      answer: `PDF передают **блоком** \`type: "document"\`. Два способа:

1. **base64** прямо в запросе (\`source: {type: "base64", media_type: "application/pdf", data: ...}\`) — блок ставят перед текстом;
2. через **Files API**: файл загружают один раз (\`client.beta.files.upload(...)\`), получают \`file_id\` и ссылаются на него из разных запросов, не пересылая заново (\`source: {type: "file", file_id: ...}\`).

Files API удобен, когда один документ используется в нескольких запросах: экономит трафик и токены загрузки. Для Files API нужен beta-заголовок (\`files-api-2025-04-14\`) — и на загрузке, и на запросе, который ссылается на файл.

Ограничения base64-PDF: до 32 МБ на запрос и ограничение по числу страниц. Для длинных документов дробите или используйте Files API. К документам можно включить **цитирование** (\`citations: {enabled: true}\`), чтобы ответ ссылался на конкретные места.`,
    },
    {
      id: 'multi-turn-stateless',
      question: 'Как вести многоходовой диалог, если API stateless?',
      answer: `Claude API **не хранит** состояние диалога на сервере — значит, **всю историю вы отправляете в каждом запросе заново**. «Память» диалога живёт у вас: вы накапливаете массив \`messages\` и шлёте его целиком.

Правила массива \`messages\`:

- первый элемент — **\`user\`**;
- роли **\`user\`/\`assistant\`** чередуются (два подряд одинаковых сервер объединит в один ход);
- на каждый ответ модели вы добавляете её \`assistant\`-сообщение в историю, затем новое \`user\`-сообщение.

\`\`\`python
messages = [
    {"role": "user", "content": "Меня зовут Алиса."},
    {"role": "assistant", "content": "Приятно познакомиться!"},
    {"role": "user", "content": "Как меня зовут?"},
]
resp = client.messages.create(model="claude-opus-5", max_tokens=256, messages=messages)
\`\`\`

Из этого вытекает и рост стоимости: чем длиннее история, тем больше входных токенов в каждом запросе — здесь помогают prompt caching (кэшировать стабильный префикс) и, для очень длинных диалогов, compaction.`,
    },
    {
      id: 'rate-limits-and-errors',
      question: 'Какие ошибки Claude API стоит повторять и как их различать?',
      answer: `Ошибки делятся на **повторяемые** и **нет**:

| Код | Тип | Повторять? |
|---|---|---|
| 400 | invalid_request_error | нет (чинить запрос) |
| 401 | authentication_error | нет (ключ) |
| 403 | permission_error | нет |
| 404 | not_found_error | нет (обычно опечатка в ID модели) |
| 429 | rate_limit_error | **да** (см. \`retry-after\`) |
| 500 / 529 | api_error / overloaded | **да** (бэкофф) |

SDK **сам** повторяет 429 и 5xx с экспоненциальной задержкой (по умолчанию \`max_retries=2\`). Различать типы нужно по **типизированным классам исключений** (\`RateLimitError\`, \`BadRequestError\`, \`NotFoundError\` …), а не по подстроке в тексте сообщения. Ловите от частного к общему:

\`\`\`python
try:
    resp = client.messages.create(...)
except anthropic.NotFoundError:   # 404 — плохой ID модели
    ...
except anthropic.RateLimitError:  # 429 — подождать и повторить
    ...
except anthropic.APIStatusError as e:
    print(e.status_code, e.message)
\`\`\``,
    },
    {
      id: 'tool-runner-vs-manual',
      question: 'Чем tool runner отличается от ручного цикла tool use?',
      answer: `**Tool runner** — хелпер SDK, который **сам крутит агентный цикл**: вызывает API, замечает запрос на инструмент, исполняет вашу функцию, отдаёт результат обратно и повторяет, пока модель не остановится. Вы пишете только сами функции-инструменты.

**Ручной цикл** вы пишете сами: while до \`stop_reason == "end_turn"\`, на каждой итерации добавляете в историю весь \`response.content\` (чтобы сохранить блоки \`tool_use\`), исполняете инструменты и возвращаете все \`tool_result\` одним \`user\`-сообщением.

Что выбрать:

- **По умолчанию — tool runner.** Он поддерживает стриминг, ограничение числа итераций и хуки (одобрение вызова, перехват результата) — «нужен контроль» редко является причиной уходить от него.
- **Ручной цикл** — когда нужен полный контроль над логикой, свой транспорт, форма запроса, которую SDK не строит, или нежелание брать beta-зависимость.

Ключевые инварианты ручного цикла: сохранять \`tool_use\`-блоки в истории и на каждый вызов возвращать \`tool_result\` с совпадающим \`tool_use_id\`.`,
    },
    {
      id: 'structured-output-limits',
      question: 'Какие ограничения у JSON-схемы для structured outputs?',
      answer: `Structured outputs (\`output_config.format\`) ограничивают ответ по **JSON Schema**, но поддерживается не вся спецификация схемы.

**Поддерживается:** базовые типы (object, array, string, integer, number, boolean, null), \`enum\`, \`const\`, \`anyOf\`, \`allOf\`, \`$ref\`/\`$def\`, строковые форматы (\`date-time\`, \`date\`, \`email\`, \`uri\`, \`uuid\` и т.д.). Для объектов **обязателен** \`additionalProperties: false\`.

**Не поддерживается:** рекурсивные схемы, числовые ограничения (\`minimum\`, \`maximum\`), ограничения длины строк (\`minLength\`, \`maxLength\`), сложные ограничения массивов.

Полезно знать:

- первая генерация с новой схемой чуть медленнее (компиляция схемы), дальше — кэш на 24 часа;
- при \`stop_reason: "refusal"\` или \`"max_tokens"\` вывод может не соответствовать схеме — проверяйте;
- structured outputs **несовместим** с цитированием (citations) и с префиллом;
- рекомендуемый способ в SDK — \`client.messages.parse()\`, он валидирует ответ по схеме автоматически. SDK Python/TypeScript сами убирают неподдерживаемые ограничения из схемы и проверяют их на клиенте.`,
    },
    {
      id: 'models-api',
      question: 'Как узнать актуальные характеристики модели программно?',
      answer: `Не полагайтесь на память и не конструируйте ID «из головы» — для **живых** данных есть **Models API**:

- \`client.models.list()\` — перечислить модели (с автопагинацией);
- \`client.models.retrieve("claude-opus-5")\` — получить одну.

У объекта модели есть \`id\`, \`display_name\`, а также (типизированные) \`max_input_tokens\` (контекстное окно), \`max_tokens\` (потолок выхода) и \`capabilities\` — дерево поддержки возможностей (vision, thinking, effort, structured outputs …) с флагом \`supported\` на каждом листе.

\`\`\`python
m = client.models.retrieve("claude-opus-5")
print(m.max_input_tokens, m.max_tokens)
print(m.capabilities["image_input"]["supported"])
\`\`\`

Используйте это, когда нужно ответить на «какое окно у X», «поддерживает ли X vision/thinking» или выбрать модель по возможностям в рантайме. Отдельного поля \`context_window\` нет — берите \`max_input_tokens\`.`,
    },
    {
      id: 'prompt-caching-placement',
      question: 'Куда ставить точки кэширования и переменные данные в промпте?',
      answer: `Так как кэш — **префиксное совпадение**, размещение решает всё. Порядок рендера: \`tools\` → \`system\` → \`messages\`.

- **Стабильное — вперёд, изменчивое — назад.** Замороженный системный промпт и детерминированный список инструментов идут первыми; таймстемпы, per-request ID и меняющийся вопрос — **после** последней точки кэширования.
- **Точку** ставят через \`cache_control: {type: "ephemeral"}\` на блоке — на последнем блоке системного промпта (закэширует tools + system) или на последнем блоке последнего хода в многоходовом диалоге.
- Общий большой префикс + разный «хвост»: точку ставят **в конце общей части**, а не всего промпта, иначе каждый запрос пишет новый кэш и ничего не читается.
- Максимум **4** точки кэширования на запрос.

\`\`\`json
"system": [
  {"type": "text", "text": "<большой стабильный промпт>",
   "cache_control": {"type": "ephemeral"}}
]
\`\`\`

Не подставляйте \`current date: X\` / \`user: Y\` в системный промпт — это ставит переменную в начало префикса и убивает кэш для всего последующего; такие данные передавайте позже, в \`messages\`.`,
    },
    {
      id: 'cache-invalidation-levels',
      question: 'Что аннулирует prompt cache сильнее всего?',
      answer: `Кэш устроен уровнями (tools → system → messages), и изменение затрагивает свой уровень **и всё, что ниже**. Сильнее всего бьют изменения в самом начале префикса:

- **Смена набора инструментов** (добавить/убрать/переставить \`tools\`) — рендерятся в позиции 0, поэтому аннулируют **весь** кэш.
- **Смена модели** — кэш привязан к модели, полный сброс.
- **Изменение системного промпта** — сбрасывает system и messages, но не tools.

Отсюда практические приёмы для агентов:

- нужен «режим»? Не меняйте набор инструментов на лету — передайте режим в содержимом сообщения или через отдельный инструмент;
- сериализуйте \`tools\` детерминированно (сортировка по имени);
- для форк-операций (суммаризация, суб-агент) копируйте \`system\`/\`tools\`/\`model\` родителя **дословно**, иначе форк промахнётся мимо кэша;
- сменить модель ради дешёвой подзадачи? Держите основной цикл на одной модели, а подзадачу отдайте суб-агенту.

Проверяйте эффект по \`usage.cache_read_input_tokens\`.`,
    },
    {
      id: 'stop-sequences',
      question: 'Что такое stop-последовательности и чем они отличаются от max_tokens?',
      answer: `**Стоп-последовательность** (\`stop_sequences\`) — строка, при генерации которой модель останавливает вывод. Это способ обрезать ответ по **содержимому**, а не по длине.

- Если сработала стоп-последовательность, приходит **\`stop_reason: "stop_sequence"\`**.
- Это отличается от **\`max_tokens\`** (упёрлись в лимит длины) и от **\`end_turn\`** (модель закончила сама).

Пример: при генерации по шаблону можно поставить стоп-строку, чтобы модель не «поехала» дальше нужного фрагмента. Но для получения структурированного вывода надёжнее не стоп-строки, а **structured outputs** (\`output_config.format\`): стоп-последовательности хрупки, а схема даёт валидный JSON гарантированно.

Итог: \`stop_sequence\` — тонкий инструмент управления границей ответа; для формата предпочтительнее схема, для потолка длины — \`max_tokens\` (и стриминг при больших значениях).`,
    },
    {
      id: 'context-editing-vs-compaction',
      question: 'Чем context editing отличается от compaction и когда что использовать?',
      answer: `Оба механизма борются с ростом контекста в долгих агентах, но по-разному:

- **Compaction (сжатие)** — сервер **суммирует** раннюю историю в компактный блок, когда диалог приближается к пределу окна. Важно возвращать обратно весь \`response.content\` (включая compaction-блоки), а не только текст.
- **Context editing (очистка)** — **удаляет** устаревшие результаты инструментов и/или блоки размышлений (\`clear_tool_uses\`, \`clear_thinking\`), а не суммирует их. Транскрипт становится «легче» без пересказа.

Как выбирать:

- **context editing** — когда старые tool-результаты уже неактуальны и их можно просто убрать;
- **compaction** — когда диалог вот-вот упрётся в окно и терять смысл истории нельзя, нужно резюме;
- часто в долгоживущих агентах используют оба, а для состояния **между** сессиями — memory tool.

Не путайте их типы: у context editing свои стратегии очистки, у compaction — отдельный механизм суммирования; это разные вещи.`,
    },
    {
      id: 'workflows-vs-agents',
      question: 'Чем workflow отличается от агента и когда что выбирать?',
      answer: `Это два способа строить системы с LLM, и их важно различать:

- **Workflow (рабочий процесс)** — LLM и инструменты оркестрируются **заранее заданным кодом** (предсказуемые ветвления). Подходит для чётко определённых задач, где нужен контроль и предсказуемость.
- **Агент** — система, где **модель сама динамически** направляет свой процесс и выбор инструментов. Подходит для открытых задач, где число шагов заранее неизвестно.

Главный принцип Anthropic — **начинать с простого**: сначала попробуйте один вызов LLM (возможно, с retrieval и примерами в контексте), затем workflow, и только при реальной необходимости — агента. Агент дороже, медленнее и менее предсказуем.

Когда стоит подниматься до агента? Проверьте: задача **действительно многошаговая** и её трудно расписать заранее; ценность оправдывает цену и задержку; модель способна на эту задачу; **ошибки можно поймать и откатить** (тесты, ревью). Если хоть один пункт «нет» — оставайтесь на уровне попроще.`,
    },
    {
      id: 'workflow-patterns',
      question: 'Какие есть базовые паттерны workflow для LLM?',
      answer: `Anthropic выделяет несколько переиспользуемых паттернов оркестрации:

- **Prompt chaining (цепочка промптов)** — задача разбивается на последовательные шаги, где каждый вызов обрабатывает результат предыдущего. Хорошо, когда задачу можно чётко декомпозировать.
- **Routing (маршрутизация)** — вход классифицируется и направляется в специализированную ветку/промпт. Полезно, когда разные типы запросов требуют разной обработки.
- **Parallelization (параллелизация)** — подзадачи выполняются одновременно: *sectioning* (разбить на независимые части) или *voting* (несколько прогонов для консенсуса/разнообразия).
- **Orchestrator–workers** — центральная модель-«оркестратор» динамически разбивает задачу и делегирует подзадачи моделям-«воркерам», затем собирает результат.
- **Evaluator–optimizer** — одна модель генерирует ответ, другая даёт обратную связь в цикле, пока результат не станет достаточно хорош.

Все они собираются из простых кирпичей (вызов модели + инструменты) и дают предсказуемость там, где полноценный агент избыточен.`,
    },
    {
      id: 'agent-when-and-risks',
      question: 'Когда оправдан полноценный агент и какие меры предосторожности нужны?',
      answer: `Агент оправдан для **открытых задач**, где невозможно заранее предсказать нужное число шагов, и где ценность результата покрывает возросшие стоимость и задержку. Это требует значительного **доверия к решениям модели**.

Меры предосторожности:

- **Тщательное тестирование в песочнице** — прогоняйте агента в изолированной среде, прежде чем давать доступ к реальным действиям.
- **Ограничители и откат** — гейты подтверждения на необратимые/дорогие действия, возможность отменить (тесты, ревью, rollback).
- **Прозрачность** — показывайте шаги планирования, чтобы поведение можно было отследить.
- **Простота дизайна** — не усложняйте без необходимости; лишние абстракции ухудшают надёжность.

Ключевой критерий перед выбором агента — **стоимость ошибки**: если ошибку легко поймать и откатить, риск приемлем; если действие необратимо и дорого — либо добавьте человека в цикл, либо оставайтесь на workflow.`,
    },
    {
      id: 'tool-design-aci',
      question: 'Как проектировать инструменты для агента (agent–computer interface)?',
      answer: `Качество инструментов для агента (ACI — **agent–computer interface**) так же важно, как UX для человека (HCI). Плохо описанные инструменты — главная причина, по которой агент ошибается.

Рекомендации Anthropic по дизайну инструментов:

- **Понятное, предписывающее описание** — указывайте не только что делает инструмент, но и **когда** его вызывать; добавляйте примеры использования и разбор пограничных случаев.
- **Формат, естественный для модели** — используйте представление данных, которое модель часто встречала; избегайте лишней «форматной» нагрузки (сложного экранирования, хрупких структур).
- **«Poka-yoke»** — проектируйте инструмент так, чтобы им было **трудно воспользоваться неправильно** (защита от ошибок): понятные обязательные параметры, разумные значения по умолчанию.
- **Тщательное тестирование** — прогоняйте много реальных вызовов и правьте описание по наблюдаемым ошибкам.

И общий принцип: инструментов должно быть **немного и по делу** — слишком большой набор путает модель. Для больших наборов есть tool search (динамическая подгрузка нужных схем).`,
    },
    {
      id: 'what-is-claude-code',
      question: 'Что такое Claude Code и на каких поверхностях он работает?',
      answer: `**Claude Code** — агентный инструмент для разработки: он читает кодовую базу, редактирует файлы, запускает команды и интегрируется с инструментами разработчика. Понимает проект целиком и работает сразу по нескольким файлам.

Он доступен на нескольких **поверхностях**, и все они используют один и тот же движок (ваши \`CLAUDE.md\`, настройки и MCP-серверы работают везде):

- **терминал (CLI)** — полнофункциональный интерфейс в командной строке;
- **расширения для IDE** (VS Code, JetBrains) — инлайн-диффы, @-упоминания, ревью плана;
- **десктоп-приложение** — визуальный просмотр диффов, параллельные сессии;
- **веб** ([claude.ai/code](https://claude.ai/code)) — запуск задач в облаке без локальной настройки.

Типичные сценарии: реализовать фичу, починить баг, написать тесты, разрулить конфликты, сделать коммит/PR. CLI **композируем** в духе Unix — можно пайпить (\`tail -200 app.log | claude -p "..."\`), запускать в CI и связывать с другими инструментами. Для полностью своих агентов есть **Agent SDK** поверх возможностей Claude Code.`,
    },
    {
      id: 'claude-md-memory',
      question: 'Что такое CLAUDE.md и как устроена память Claude Code?',
      answer: `**\`CLAUDE.md\`** — markdown-файл в проекте, который Claude Code читает **в начале каждой сессии**. В него кладут то, что модель должна знать постоянно: стандарты кода, архитектурные решения, предпочитаемые библиотеки, чек-листы ревью, команды сборки.

Память Claude Code многоуровневая:

- **Проектный \`CLAUDE.md\`** (в корне репозитория) — общий для команды, коммитится в git;
- **Пользовательская память** — личные предпочтения, общие для всех ваших проектов;
- вложенные \`CLAUDE.md\` в подкаталогах могут уточнять инструкции для своей части кода.

Отдельно есть **auto memory**: Claude Code сам сохраняет полезные находки (команды сборки, инсайты при отладке) между сессиями — писать их вручную не нужно.

Практический смысл: \`CLAUDE.md\` — это «инструкция для ассистента», которая делает поведение предсказуемым и убирает необходимость повторять контекст в каждой сессии.`,
    },
    {
      id: 'claude-code-config',
      question: 'Как настраивают и расширяют Claude Code (settings, slash-команды, hooks, subagents, MCP)?',
      answer: `Claude Code настраивается несколькими механизмами:

- **\`settings.json\`** — конфигурация поведения и **разрешений** (что модели позволено делать без спроса — allowlist инструментов/команд), переменные окружения, хуки.
- **Slash-команды и Skills** — переиспользуемые рабочие процессы, которые можно расшарить команде (например, \`/review-pr\`, \`/deploy-staging\`).
- **Hooks (хуки)** — shell-команды, запускаемые **до/после** действий Claude Code: авто-форматирование после каждого редактирования, линт перед коммитом. Именно хуки реализуют «каждый раз, когда…»-автоматизации.
- **Subagents (суб-агенты)** — можно порождать несколько агентов, работающих над разными частями задачи параллельно; ведущий агент координирует и объединяет результаты.
- **MCP (Model Context Protocol)** — открытый стандарт подключения внешних источников/инструментов (Google Drive, Jira, Slack, свои сервисы). Через MCP Claude Code читает доки и обновляет тикеты.

Итог: **инструкции** задаёт \`CLAUDE.md\`, **разрешения и хуки** — \`settings.json\`, **внешние инструменты** — MCP, а **масштаб** — суб-агенты.`,
    },
    {
      id: 'why-evals',
      question: 'Зачем нужны eval\'ы и что такое eval-driven разработка?',
      answer: `**Eval (оценка)** — это набор тест-кейсов, по которым измеряют качество ответов модели объективно, а не «на глаз». Без eval'ов невозможно понять, улучшил ли правка промпта результат или ухудшил.

**Eval-driven разработка** — итеративный цикл:

\`\`\`
Тест-кейсы → предварительный промпт → итерации (тест + правка) → финальная валидация → релиз
\`\`\`

Ключевые принципы:

- **Критерии успеха по SMART** — конкретные, измеримые, достижимые, релевантные (например, «F1 ≥ 0.85 на 10 000 постов», а не «модель хорошо классифицирует»).
- **Объём важнее идеальности** — много автоматически проверяемых кейсов дают лучший сигнал, чем несколько ручных.
- **Покрывайте пограничные случаи** — опечатки, сарказм, неоднозначность, длинный вход, вредоносный ввод.
- **Многомерность** — реальные приложения оценивают сразу по нескольким осям: точность + тон + задержка + стоимость.

Eval'ы превращают «доработку промпта» из угадывания в измеримый инженерный процесс.`,
    },
    {
      id: 'eval-grading-methods',
      question: 'Какими способами оценивают ответы модели (grading)?',
      answer: `Способы проверки ответов делятся на три группы:

- **Кодовая проверка (exact match / code-graded)** — сравнение по коду: точное совпадение метки, эмбеддинги/косинусная близость (консистентность), ROUGE (близость к эталонному саммари). Быстро, дёшево, детерминированно — идеально для классификации и задач с чётким ответом.
- **Оценка человеком** — самая гибкая, но дорогая и не масштабируется; используют как эталон и для тонких случаев.
- **LLM-оценка (model-graded)** — другая модель судит по критерию: шкала Ликерта 1–5 (тон, эмпатия), бинарная классификация (например, «есть ли PII?»), порядковая шкала (насколько использован контекст). Масштабируется на субъективные качества.

Рекомендации:

- по возможности **автоматизируйте** (кодовая или LLM-оценка) — так можно прогнать сотни/тысячи кейсов;
- для LLM-оценки **берите отдельную модель-судью** (и давайте ей чёткую рубрику с примерами);
- на «нечёткие» темы всё равно задавайте числовую метрику (например, «< 0.1% токсичности»).`,
    },
    {
      id: 'debugging-prompts',
      question: 'Как отлаживать и улучшать промпт по результатам eval\'ов?',
      answer: `Отладка промпта — это работа по **паттернам ошибок**, а не по одиночным случаям:

1. **Прогоните базовый промпт** на тестовом наборе и измерьте против критериев успеха.
2. **Сгруппируйте провалы** — найдите общий тип ошибки (путает две метки? теряет контекст? уходит в многословие?).
3. **Правьте прицельно** — уточните инструкцию под конкретный тип провала; часто **позитивные примеры** желаемого работают лучше, чем список запретов.
4. **Проверьте на held-out наборе** — финальную валидацию делают на данных, которые не участвовали в итерациях, чтобы не «подогнать» промпт.

Полезные приёмы: структурируйте промпт (секции/XML-теги), давайте few-shot примеры проблемных случаев, а для строгого формата — structured outputs вместо уговоров. И помните: смена модели или большие правки промпта **инвалидируют кэш** и меняют поведение — перепроверяйте метрики после каждого изменения.`,
    },
    {
      id: 'prompt-injection',
      question: 'Что такое prompt injection и как от неё защищаться?',
      answer: `**Prompt injection** — атака, при которой во **внешних, недоверенных данных** (веб-страница, документ, письмо, результат инструмента, содержимое файла) спрятаны инструкции, пытающиеся перехватить поведение модели («игнорируй прежние указания и сделай X»).

Базовый принцип защиты: **всё, что пришло из внешних источников и инструментов, — это данные, а не команды.** Модель не должна исполнять инструкции, найденные внутри такого контента.

Практические меры:

- **Разделяйте роли и данные** — недоверенный контент кладите в явно обозначенные секции (например, \`<document>...</document>\`) и в промпте указывайте, что это данные для анализа, а не инструкции;
- **Гейты на действия** — необратимые/чувствительные операции (отправка, удаление, платежи) подтверждайте, особенно если триггер пришёл из внешнего контента;
- **Минимальные права** — инструменты и ключи с наименьшими необходимыми правами, чтобы ограничить ущерб;
- **Не доверяйте ссылкам/адресам из контента** — не отправляйте данные на endpoint, «подсказанный» самим недоверенным текстом.

Особенно важно в агентах и tool use, где модель действует по результатам, пришедшим извне.`,
    },
    {
      id: 'guardrails',
      question: 'Как ставить ограничители (guardrails) на вход и выход модели?',
      answer: `**Guardrails** — механизмы, удерживающие поведение приложения в допустимых рамках. Их ставят на трёх уровнях:

- **Системный промпт** — задаёт роль, правила и границы: что модель делает, чего не делает, как отвечать на выходящее за рамки. Это первый и самый дешёвый ограничитель.
- **Валидация входа** — проверяйте и санитизируйте пользовательский ввод; недоверенный контент подавайте как данные, а не как инструкции (см. prompt injection).
- **Модерация/валидация выхода** — проверяйте ответ перед показом или действием: классификатор нежелательного контента, проверка формата (structured outputs), бинарная LLM-проверка («содержит ли PII?»).

Дополнительно:

- **Обрабатывайте \`stop_reason: "refusal"\`** — отказ безопасности приходит с HTTP 200; проверяйте stop_reason до чтения content;
- **Человек в цикле** для высокорисковых действий;
- **Тестируйте ограничители eval'ами** — даже «нечёткую» безопасность квантуйте метрикой (доля флагнутых ответов).`,
    },
    {
      id: 'responsible-use',
      question: 'Что относится к ответственному использованию Claude в приложениях?',
      answer: `Ответственное использование — это набор практик, снижающих вред и риски:

- **Не храните секреты и лишние ПДн** в промптах, памяти или файлах памяти — они попадают в историю и могут «всплыть» в будущих ответах; ключи и пароли держите в переменных окружения/секрет-хранилище.
- **Аккуратно с персональными данными (PII/PHI)** — минимизируйте сбор, проверяйте выход на утечку чувствительной информации, соблюдайте регуляторные требования;
- **Человеческий надзор для высоких ставок** — решения с серьёзными последствиями (медицина, финансы, право) не должны исполняться моделью без проверки человеком;
- **Прозрачность для пользователя** — обозначайте, что он взаимодействует с ИИ, и не выдавайте вероятностный ответ за гарантированный факт;
- **Не полагайтесь на модель как на источник истины** — она может «галлюцинировать»; для актуальных/проверяемых фактов давайте инструменты (web search) и просите опираться на источники;
- **Уважайте отказы безопасности** — если модель отказала (\`refusal\`), не пытайтесь обойти это ради запрещённого результата.

Эти практики — часть домена «Security & Safety» экзамена и просто здравого инженерного смысла.`,
    },
    // Импортированный практический набор (multi-response вопросы), см. атрибуцию в ccdvImported-questions.js
    ...ccdvImportedQuestions,
    // Учебные карточки из single-answer вопросов практического набора (по одной на концепцию)
    ...ccdvStudyQuestions,
  ].map((q) => ({ ...q, subtopic: q.subtopic ?? SUBTOPIC[q.id] ?? 'General' })),
};
