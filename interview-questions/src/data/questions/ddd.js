// Категория: ddd
export const ddd = {
  id: 'ddd',
  title: 'Domain-Driven Design',
  icon: '🧭',
  description: 'Ubiquitous Language, Bounded Context, Aggregate, тактические и стратегические паттерны',
  questions: [
    {
      id: 'what-is-ddd',
      difficulty: 'easy',
      tags: ['basics', 'strategic'],
      related: ['ddd-problems', 'ubiquitous-language', 'bounded-context'],
      question: 'Что такое Domain-Driven Design (DDD)?',
      answer: `**Domain-Driven Design** — подход к проектированию сложного ПО, ставящий в центр **предметную область (domain)** и её модель, а не технические слои. Ключевая идея — строить код вокруг бизнес-модели, выработанной совместно с экспертами предметной области.

DDD делится на два уровня:

- **Стратегический** — как разбить большую систему на части: **Bounded Context**, **Ubiquitous Language**, карта контекстов (Context Map), Anti-Corruption Layer;
- **Тактический** — как устроен код внутри контекста: **Entity**, **Value Object**, **Aggregate**, Repository, Domain Service, Domain Event, Factory.

DDD оправдан там, где **сложность именно доменная** (богатые правила, инварианты, много терминов). Для простого CRUD он избыточен — достаточно anemic-модели.

Термин ввёл Эрик Эванс в книге *«Domain-Driven Design»* (2003).`,
    },
    {
      id: 'ddd-problems',
      difficulty: 'easy',
      tags: ['basics', 'strategic'],
      related: ['what-is-ddd', 'ubiquitous-language'],
      question: 'Какие проблемы решает DDD?',
      answer: `DDD борется с типичными болезнями сложных систем:

- **Разрыв между бизнесом и кодом** — аналитики, разработчики и эксперты говорят на разных языках; в коде теряется смысл бизнес-правил. DDD вводит **Ubiquitous Language** — общий язык.
- **Размазанная бизнес-логика** — правила разбросаны по контроллерам, сервисам и SQL (anemic domain model). DDD собирает логику в модель домена (rich model).
- **Большой ком грязи (big ball of mud)** — одна модель на всю систему, где всё связано со всем. DDD режет систему на **Bounded Context** с явными границами.
- **Конфликт терминов** — «клиент» в продажах и в поддержке значит разное. Контексты позволяют разным моделям сосуществовать.
- **Хрупкая согласованность** — инварианты нарушаются из-за неявных границ транзакций. **Aggregate** задаёт границу согласованности.

DDD — это не про фреймворк, а про **управление сложностью** через модель и границы.`,
    },
    {
      id: 'ubiquitous-language',
      difficulty: 'medium',
      tags: ['strategic'],
      related: ['what-is-ddd', 'bounded-context'],
      question: 'Что такое Ubiquitous Language (единый язык)?',
      answer: `**Ubiquitous Language** — общий язык терминов предметной области, на котором говорят **и эксперты домена, и разработчики**, и который **напрямую отражён в коде** (имена классов, методов, событий).

Смысл: устранить перевод «бизнес → аналитик → разработчик → код», на каждом шаге которого теряется смысл. Если бизнес говорит *«заказ отгружается (Order is shipped)»*, то в коде появляется \`Order.ship()\` и событие \`OrderShipped\`, а не \`updateStatus(3)\`.

Свойства:

- вырабатывается **совместно** и постоянно уточняется;
- **привязан к Bounded Context** — вне контекста тот же термин может значить другое;
- живёт в разговорах, документации и коде синхронно (рассинхрон — сигнал деградации модели).

Признак хорошего языка: бизнес-эксперт, читая имена в коде/тестах, понимает, что там происходит.`,
    },
    {
      id: 'bounded-context',
      difficulty: 'medium',
      tags: ['strategic'],
      related: ['ubiquitous-language', 'identify-bounded-contexts', 'same-term-different-context'],
      question: 'Что такое Bounded Context (ограниченный контекст)?',
      answer: `**Bounded Context** — явная граница, внутри которой конкретная модель домена и её Ubiquitous Language **консистентны и однозначны**. За границей действует другая модель со своим языком.

Пример: в контексте *«Продажи»* \`Product\` — это цена, скидки, доступность; в контексте *«Склад»* тот же \`Product\` — это габариты, вес, место хранения. Пытаться объединить их в один класс — ошибка; это **два разных \`Product\`** в двух контекстах.

Зачем нужен:

- одна модель на всю систему неизбежно превращается в «большой ком грязи»;
- контекст ограничивает область, где термин имеет один смысл;
- часто (не всегда) Bounded Context ложится на границу **микросервиса** или отдельного модуля.

Отношения между контекстами описывает **Context Map** (Partnership, Customer-Supplier, Conformist, Anti-Corruption Layer, Shared Kernel и др.).`,
    },
    {
      id: 'identify-bounded-contexts',
      difficulty: 'hard',
      tags: ['strategic', 'advanced'],
      related: ['bounded-context', 'same-term-different-context'],
      question: 'Как выделять Bounded Context в системе?',
      answer: `Границы контекстов ищут **по смыслу языка**, а не по техническим слоям. Признаки, что перед вами разные контексты:

- **один термин значит разное** («заказ» в оформлении и в доставке) — сильнейший сигнал границы;
- **разные группы экспертов / отделы** отвечают за разные части (продажи, склад, биллинг);
- **разные причины изменений** — части меняются по независимым бизнес-поводам (принцип, близкий к SRP на уровне системы);
- **разные модели данных и жизненные циклы** сущностей.

Полезные техники:

- **Event Storming** — совместный воркшоп: раскладываем доменные события по времени, кластеры событий проявляют границы;
- анализ **Ubiquitous Language** — где язык «ломается», там граница;
- **Domain Storytelling**, анализ бизнес-возможностей (capabilities).

Ошибка — резать по техническим сущностям («сервис User», «сервис Order») вместо бизнес-возможностей.`,
    },
    {
      id: 'same-term-different-context',
      difficulty: 'medium',
      tags: ['strategic'],
      related: ['bounded-context', 'ubiquitous-language'],
      question: 'Может ли один бизнес-термин означать разное в разных контекстах?',
      answer: `Да — и это **норма**, а не проблема. Один и тот же термин в разных Bounded Context часто обозначает разные модели.

Классический пример — **Customer**:

- в контексте *«Продажи»* — история покупок, скидки, сегмент;
- в контексте *«Поддержка»* — тикеты, каналы связи, приоритет;
- в контексте *«Биллинг»* — платёжные реквизиты, задолженность.

Попытка сделать **один универсальный класс \`Customer\`** на все случаи приводит к раздутой модели, которая тянет чужие поля и связывает несвязанное. Правильнее — **своя модель \`Customer\` в каждом контексте**, связанная лишь общим идентификатором.

Именно поэтому Ubiquitous Language **привязан к контексту**: однозначность гарантируется только внутри границы. Согласование смыслов между контекстами — задача Context Map и трансляции (Anti-Corruption Layer).`,
    },
    {
      id: 'entity',
      difficulty: 'easy',
      tags: ['tactical'],
      related: ['value-object', 'entity-vs-value-object'],
      question: 'Что такое Entity (сущность)?',
      answer: `**Entity** — объект домена, обладающий **идентичностью (identity)**, которая сохраняется на протяжении всего его жизненного цикла, даже если меняются атрибуты.

Два объекта Entity равны, если **равны их идентификаторы**, а не значения полей. Пользователь с изменившимся именем и адресом — это тот же пользователь.

\`\`\`java
class Order {
    private final OrderId id;      // идентичность
    private OrderStatus status;    // изменяемое состояние
    // equals/hashCode — только по id
}
\`\`\`

Признаки Entity:

- есть уникальный ID (часто присваивается при создании);
- **изменяемое** состояние (mutable) во времени;
- важна непрерывность («тот же самый» объект), а не только текущие значения.

Противоположность — **Value Object**, который сравнивается по значению и не имеет идентичности.`,
    },
    {
      id: 'value-object',
      difficulty: 'easy',
      tags: ['tactical'],
      related: ['entity', 'entity-vs-value-object', 'value-object-immutable'],
      question: 'Что такое Value Object (объект-значение)?',
      answer: `**Value Object** — объект, который **не имеет идентичности** и полностью определяется своими **значениями**. Два Value Object равны, если равны все их поля.

Примеры: \`Money(100, "USD")\`, \`Address\`, \`DateRange\`, \`Email\`, \`Coordinates\`. Купюра в 100 рублей взаимозаменяема с любой другой такой же — конкретный экземпляр не важен.

\`\`\`java
record Money(BigDecimal amount, Currency currency) {
    Money add(Money other) {
        if (!currency.equals(other.currency))
            throw new IllegalArgumentException("currency mismatch");
        return new Money(amount.add(other.amount), currency);
    }
}
\`\`\`

Свойства:

- равенство и hashCode — **по значению** (в Java удобно через \`record\`);
- как правило **неизменяемы** (immutable);
- инкапсулируют правила своего типа (валидацию, операции), убирая «примитивную одержимость» (\`String email\`, \`BigDecimal amount\`).`,
    },
    {
      id: 'entity-vs-value-object',
      difficulty: 'medium',
      tags: ['tactical'],
      related: ['entity', 'value-object'],
      question: 'В чём разница между Entity и Value Object?',
      answer: `Различие — в **природе равенства и идентичности**.

| Признак | Entity | Value Object |
|---|---|---|
| Идентичность | есть (ID) | нет |
| Равенство | по идентификатору | по значению всех полей |
| Изменяемость | обычно mutable | обычно immutable |
| Жизненный цикл | важна непрерывность во времени | заменяется целиком |
| Пример | \`Order\`, \`User\`, \`Account\` | \`Money\`, \`Address\`, \`Email\` |

Простой тест: **«Важно ли, что это именно этот экземпляр, или важны только значения?»** Если два объекта с одинаковыми полями — это «одно и то же» → Value Object; если они могут быть разными сущностями с одинаковыми атрибутами → Entity.

Один и тот же концепт может быть тем или другим в зависимости от контекста: адрес доставки — обычно Value Object, но «объект недвижимости» с историей — Entity.`,
    },
    {
      id: 'value-object-immutable',
      difficulty: 'medium',
      tags: ['tactical'],
      related: ['value-object'],
      question: 'Почему Value Object обычно делают неизменяемым?',
      answer: `Неизменяемость Value Object даёт сразу несколько выгод:

- **Безопасность разделения** — один экземпляр можно свободно передавать и переиспользовать; никто не изменит его «из-под ног». Иначе изменение общего \`Address\` в одном месте неожиданно затронуло бы другие.
- **Целостность по построению** — валидность проверяется один раз в конструкторе; дальше объект гарантированно корректен, инварианты нельзя нарушить сеттером.
- **Корректное равенство** — раз объект используется как «значение» (в т.ч. как ключ \`Map\`/элемент \`Set\`), его hashCode не должен меняться после создания.
- **Потокобезопасность** — immutable-объекты безопасны в конкурентной среде без синхронизации.

«Изменение» моделируется как **создание нового объекта**: \`money.add(other)\` возвращает новый \`Money\`, а не мутирует старый. В Java это естественно выражается через \`record\`.`,
    },
    {
      id: 'aggregate',
      difficulty: 'medium',
      tags: ['tactical'],
      related: ['aggregate-root', 'invariant', 'aggregate-boundaries-why'],
      question: 'Что такое Aggregate (агрегат)?',
      answer: `**Aggregate** — кластер связанных Entity и Value Object, который рассматривается как **единое целое** с точки зрения изменений и согласованности. У агрегата есть корень — **Aggregate Root**.

Пример: агрегат \`Order\` включает сам заказ (root) и его \`OrderLine\` (позиции). Позиции не существуют и не изменяются в отрыве от заказа.

Ключевые правила:

- агрегат — **граница согласованности (consistency boundary)**: все инварианты внутри него всегда истинны после каждой операции;
- агрегат — **граница транзакции**: одна транзакция изменяет **один** агрегат (правило «one aggregate per transaction»);
- внешний мир обращается к содержимому **только через корень**;
- ссылки на другие агрегаты — **по идентификатору**, а не прямой ссылкой на объект.

Согласованность между разными агрегатами — **eventual** (через доменные события), а не в одной транзакции.`,
    },
    {
      id: 'aggregate-root',
      difficulty: 'medium',
      tags: ['tactical'],
      related: ['aggregate', 'reference-aggregate-root', 'protect-invariants'],
      question: 'Что такое Aggregate Root (корень агрегата)?',
      answer: `**Aggregate Root** — единственная Entity агрегата, которая служит **точкой входа** ко всему агрегату. Внешний код может получить и изменить содержимое агрегата **только через корень**.

Обязанности корня:

- **гарантировать инварианты** всего агрегата: любые изменения внутренних объектов идут через методы корня, которые проверяют правила;
- контролировать жизненный цикл внутренних сущностей (создание/удаление \`OrderLine\` — только через \`Order\`);
- иметь **глобальный идентификатор**; у внутренних сущностей идентичность лишь локальная (в пределах агрегата).

\`\`\`java
class Order {                 // Aggregate Root
    private List<OrderLine> lines;
    void addLine(Product p, int qty) {   // единственный путь изменения
        if (status != DRAFT) throw new IllegalStateException();
        lines.add(new OrderLine(p, qty));
        recalcTotal();                    // инвариант поддержан
    }
}
\`\`\`

Repository работает **на уровне корня**: сохраняет/загружает агрегат целиком.`,
    },
    {
      id: 'aggregate-boundaries-why',
      difficulty: 'hard',
      tags: ['tactical', 'advanced'],
      related: ['aggregate', 'invariant', 'choose-aggregate-boundaries'],
      question: 'Зачем нужны границы агрегата?',
      answer: `Граница агрегата отвечает на вопрос: **что должно быть согласовано атомарно (в одной транзакции), а что — лишь в конечном счёте (eventually)**.

Что даёт граница:

- **Защита инвариантов** — правила, охватывающие несколько объектов (например, «сумма позиций = итог заказа»), можно гарантировать только внутри единой границы согласованности.
- **Управление транзакциями** — изменяя один агрегат за транзакцию, избегаем распределённых блокировок и длинных транзакций; система лучше масштабируется.
- **Снижение конкуренции (contention)** — мелкие агрегаты реже конфликтуют при оптимистичных блокировках.
- **Ясность модели** — понятно, где сильная согласованность, а где допустима eventual.

Слишком большой агрегат → длинные транзакции, конфликты версий, плохая масштабируемость. Слишком мелкий → инвариант, который нельзя гарантировать в одной транзакции. Поэтому выбор границ — центральное проектное решение.`,
    },
    {
      id: 'choose-aggregate-boundaries',
      difficulty: 'hard',
      tags: ['tactical', 'advanced'],
      related: ['aggregate-boundaries-why', 'invariant'],
      question: 'Как выбирать границы агрегата?',
      answer: `Границы агрегата определяются **инвариантами** и требованиями к согласованности, а не структурой БД или UI.

Практические правила:

- **От инвариантов**: что должно быть истинно **немедленно после каждой операции** — то в один агрегат; что допустимо согласовать позже — в разные.
- **Делайте агрегаты маленькими**: большинство должно состоять из корня и нескольких Value Object. Маленькие агрегаты = короткие транзакции, меньше конфликтов, лучшая масштабируемость.
- **Ссылки по ID**: на другие агрегаты — только по идентификатору, не тяните весь чужой объект в границу.
- **Правило одной транзакции**: если для бизнес-операции нужно менять два агрегата атомарно — вероятно, граница проведена неверно (или согласованность на самом деле eventual).
- **True invariant vs. отчётность**: если правило — это агрегирующий отчёт (например, «топ клиентов»), его не нужно защищать транзакцией.

Типичная ошибка — тянуть в агрегат всё «связанное» (заказ + клиент + товары), получая огромную границу. Связь ≠ принадлежность к одному агрегату.`,
    },
    {
      id: 'invariant',
      difficulty: 'medium',
      tags: ['tactical'],
      related: ['aggregate', 'protect-invariants'],
      question: 'Что такое инвариант?',
      answer: `**Инвариант** — бизнес-правило, которое должно быть **истинным всегда** (в любой согласованный момент, между транзакциями), а не только временами.

Примеры:

- «Сумма позиций заказа равна итоговой сумме»;
- «Баланс счёта не может быть отрицательным»;
- «У подтверждённого заказа есть хотя бы одна позиция»;
- «Дата окончания брони позже даты начала».

Особенности:

- инвариант часто охватывает **несколько объектов** внутри агрегата (поэтому его и защищают на уровне агрегата);
- он должен восстанавливаться **до конца каждой операции**, а не «когда-нибудь»;
- инвариант — не то же самое, что валидация ввода: это правило целостности **модели**, поддерживаемое её собственными методами.

Именно инварианты определяют, где проходят границы агрегата: всё, что должно быть согласовано атомарно ради инварианта, живёт в одной границе.`,
    },
    {
      id: 'protect-invariants',
      difficulty: 'medium',
      tags: ['tactical'],
      related: ['invariant', 'aggregate-root', 'reference-aggregate-root'],
      question: 'Почему инварианты нужно защищать внутри агрегата?',
      answer: `Если инвариант можно нарушить в обход модели, он перестаёт быть гарантией и превращается в «пожелание», которое рано или поздно нарушат.

Поэтому агрегат:

- **инкапсулирует внутренности** — не отдаёт наружу изменяемые ссылки на \`OrderLine\`; любое изменение идёт через методы корня, которые проверяют правила;
- **не имеет «сырых» сеттеров** — вместо \`setStatus()\` есть осмысленные операции \`confirm()\`, \`cancel()\`, внутри которых проверяются предусловия;
- **держит согласованность в границе транзакции** — после коммита агрегат всегда валиден.

Если же бизнес-логику размазать по сервисам и позволить менять внутренние объекты напрямую (anemic model), проверки инвариантов **дублируются и разъезжаются**: один путь проверяет, другой забыл. Централизация правил в агрегате делает нарушение инварианта **невозможным по построению**, а не «отловленным где-то в сервисе».`,
    },
    {
      id: 'reference-aggregate-root',
      difficulty: 'hard',
      tags: ['tactical', 'advanced'],
      related: ['aggregate-root', 'protect-invariants'],
      question: 'Почему внешние объекты должны ссылаться только на корень агрегата?',
      answer: `Если внешний код держит ссылку на **внутренний** объект агрегата (например, на \`OrderLine\`) и меняет его напрямую, он обходит корень — а значит, обходит и **проверку инвариантов**. Агрегат теряет контроль над собственной согласованностью.

Поэтому правило: снаружи виден и доступен **только Aggregate Root**.

- изменения внутренних сущностей — только через методы корня, где действуют бизнес-правила;
- на **другие** агрегаты ссылаются **по идентификатору** (\`customerId\`), а не прямой объектной ссылкой — это:
  - удерживает границы агрегатов раздельными (не тянет чужой граф объектов),
  - упрощает загрузку/сохранение (Repository работает с одним агрегатом),
  - естественно ложится на распределённую систему, где «прямой ссылки» между сервисами не бывает.

Итог: единая точка входа = единая точка контроля инвариантов и единая единица загрузки/сохранения/транзакции.`,
    },
    {
      id: 'bounded-context-communication',
      difficulty: 'hard',
      tags: ['strategic', 'advanced'],
      related: ['bounded-context', 'anti-corruption-layer', 'shared-model-danger'],
      question: 'Как взаимодействуют между собой Bounded Context?',
      answer: `Контексты интегрируются через явные, документированные связи; их совокупность — **Context Map**. Основные типы отношений:

- **Partnership** — две команды координируют модели и релизы совместно;
- **Customer–Supplier** — вышестоящий (upstream) контекст учитывает потребности нижестоящего (downstream);
- **Conformist** — downstream принимает модель upstream как есть (без трансляции);
- **Anti-Corruption Layer (ACL)** — downstream ставит слой трансляции, защищающий свою модель от чужой;
- **Open Host Service / Published Language** — upstream публикует стабильный API/контракт (например, REST + схема, события с общей схемой);
- **Shared Kernel** — небольшая общая часть модели, разделяемая двумя контекстами (рискованно, требует жёсткой координации);
- **Separate Ways** — интеграции нет вовсе.

Технически связь реализуют **синхронно** (REST/gRPC) или **асинхронно** (доменные события через брокер). На каждой границе важна **трансляция** между разными Ubiquitous Language.`,
    },
    {
      id: 'anti-corruption-layer',
      difficulty: 'hard',
      tags: ['strategic', 'advanced'],
      related: ['bounded-context-communication', 'shared-model-danger'],
      question: 'Что такое Anti-Corruption Layer (ACL)?',
      answer: `**Anti-Corruption Layer** — слой трансляции на границе контекста, который **переводит чужую модель в термины вашей** и не даёт понятиям внешней системы «протечь» в вашу модель.

Зачем: если напрямую использовать модель чужого/legacy-сервиса, его концепции и странности расползутся по вашему коду, и ваш Ubiquitous Language деградирует. ACL изолирует эту зависимость.

Как устроен:

- набор адаптеров/фасадов и мапперов, преобразующих внешние DTO/термины в ваши доменные объекты (и обратно);
- нередко реализует паттерны Adapter + Facade + Translator;
- ставится на стороне **downstream**-контекста.

\`\`\`text
[Ваш домен] ← ACL (translate) ← [Внешний / legacy сервис]
\`\`\`

Плюс: смена внешней системы затрагивает только ACL. Минус: дополнительный код и поддержка маппинга — оправдано, когда чужая модель существенно отличается или нестабильна.`,
    },
    {
      id: 'shared-model-danger',
      difficulty: 'hard',
      tags: ['strategic', 'advanced'],
      related: ['bounded-context', 'anti-corruption-layer', 'same-term-different-context'],
      question: 'Почему общая модель между сервисами/контекстами часто опасна?',
      answer: `Соблазн вынести «общий» доменный класс (или общую библиотеку с сущностями) в разделяемую зависимость на несколько сервисов приводит к серьёзным проблемам:

- **Скрытая связанность (coupling)** — сервисы, деля модель, перестают быть независимыми: изменение общей модели заставляет менять и передеплоивать всех. Теряется главное преимущество разделения на контексты/сервисы.
- **Компромиссная модель** — один класс на всех вынужден удовлетворять противоречивые потребности и обрастает лишними полями (см. пример с \`Customer\`). Он не подходит толком никому.
- **Разъезжающиеся смыслы** — один термин в разных контекстах значит разное; общая модель это отрицает и порождает баги.
- **Организационный тормоз** — общий код требует постоянной координации команд.

Правильнее — у каждого контекста **своя модель**, а на границах — трансляция (ACL) и обмен через **контракты/события** (Published Language), а не через общий доменный тип. Единственное узкое исключение — намеренный **Shared Kernel** с жёсткой координацией.`,
    },
  ],
};
