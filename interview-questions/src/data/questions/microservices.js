// Категория: microservices
export const microservices = {
    id: 'microservices',
    title: 'Микросервисы и Docker',
    icon: '🧱',
    description: 'Архитектура, контейнеризация, паттерны',
    questions: [
      {
        id: 'monolith-vs-microservices',
        difficulty: 'easy',
        tags: ['basics'],
        related: ['service-boundaries', 'monolith-migration'],
        question: 'В чем разница между монолитом и микросервисами?',
        answer: `**Монолит** — приложение как единый деплоймент: один процесс, одна БД, один релиз.

Плюсы: простота разработки/отладки/деплоя на старте, транзакции ACID, нет сетевых накладных расходов. Минусы: масштабирование только целиком, длинные релизы при росте, деградация модульности («big ball of mud»), привязка к одному стеку.

**Микросервисы** — набор небольших независимых сервисов, каждый:

- отвечает за свой бизнес-домен (bounded context из DDD);
- имеет **собственную БД** (database per service);
- деплоится и масштабируется независимо;
- общается по сети (REST, gRPC, сообщения).

Плюсы: независимые команды и релизы, точечное масштабирование, отказоустойчивость (падение одного ≠ падение всех), свобода технологий. Минусы: **распределённая сложность** — сетевые сбои, eventual consistency вместо ACID, распределённые транзакции, сложность отладки и тестирования, DevOps-инфраструктура (оркестрация, мониторинг, трейсинг).

**Правило**: начинать с модульного монолита (modular monolith), выделять микросервисы при реальной необходимости (разные скорости изменений, нагрузка, команды). «Microservices are not a free lunch» — цена в операционной сложности.

Промежуточный шаг — **распил монолита** паттерном Strangler Fig: постепенное выведение функциональности в сервисы.`,
      },
      {
        id: 'microservices-communication',
        difficulty: 'medium',
        tags: ['communication'],
        related: ['sync-vs-async', 'loose-coupling'],
        question: 'Какие способы взаимодействия между микросервисами существуют?',
        answer: `**Синхронное взаимодействие** (запрос-ответ, вызывающий ждёт):

- **REST/HTTP + JSON** — простота, универсальность; стандарт для внешних API;
- **gRPC** — бинарный Protobuf, HTTP/2, строгие контракты, стриминг; в 5-10 раз быстрее REST — выбор для межсервисного общения;
- **GraphQL** — гибкие выборки, обычно на BFF/API Gateway.

Минусы синхронного: каскадные отказы (сервис ждёт сервис, который ждёт сервис...), временнáя связанность (оба должны быть доступны).

**Асинхронное взаимодействие** (через брокер сообщений):

- **события** (Kafka, RabbitMQ): сервис публикует «OrderCreated», подписчики реагируют независимо;
- **команды/очереди задач**: точечная доставка одному исполнителю;
- плюсы: слабая связанность, буферизация нагрузки, устойчивость к недоступности потребителя;
- минусы: eventual consistency, сложнее отладка, дубликаты (нужна идемпотентность).

**Ключевые практики:**

- **API-контракты**: OpenAPI/Protobuf схемы, contract testing (Pact), обратная совместимость;
- **service discovery**: Kubernetes DNS, Eureka, Consul;
- **устойчивость**: таймауты, ретраи с backoff, circuit breaker (Resilience4j);
- **наблюдаемость**: распределённый трейсинг (OpenTelemetry, correlation id).

Рекомендация: события для интеграции доменов, синхронные вызовы — для запросов данных в реальном времени.`,
      },
      {
        id: 'microservices-patterns',
        difficulty: 'medium',
        tags: ['patterns'],
        related: ['ms-resilience'],
        question: 'Какие паттерны микросервисной архитектуры вы знаете?',
        answer: `**API Gateway** — единая точка входа: маршрутизация, аутентификация, rate limiting, агрегация ответов (Spring Cloud Gateway, Kong, nginx).

**Circuit Breaker (предохранитель)** — защита от каскадных отказов: после N ошибок «размыкается» и сразу возвращает ошибку/fallback, периодически проверяя восстановление (Closed → Open → Half-Open). Реализация: Resilience4j.

**Saga** — распределённые транзакции через последовательность локальных транзакций с **компенсирующими действиями** при сбое:

- **хореография** — сервисы реагируют на события друг друга;
- **оркестрация** — центральный координатор (Camunda, Temporal).

**Transactional Outbox** — атомарность «запись в БД + отправка события»: событие пишется в таблицу outbox в той же транзакции, отдельный процесс публикует его в брокер (+ CDC/Debezium).

**CQRS** — разделение моделей записи (commands) и чтения (queries); часто с **Event Sourcing** (состояние = последовательность событий).

**Database per Service** — у каждого сервиса своя БД; чужие данные — только через API.

**Service Discovery**, **Config Server** — инфраструктурные паттерны (Spring Cloud Config, Kubernetes ConfigMaps).

**Sidecar / Service Mesh** — вынос сетевой логики (mTLS, ретраи, трейсинг) в прокси рядом с сервисом (Istio, Linkerd).

**BFF (Backend for Frontend)** — отдельный API-слой под каждый тип клиента (web, mobile).

**Bulkhead** — изоляция ресурсов (отдельные пулы потоков/соединений), чтобы отказ одной зависимости не исчерпал ресурсы всего сервиса.`,
      },
      {
        id: 'kubernetes',
        difficulty: 'medium',
        tags: ['deployment'],
        related: [],
        question: 'Что такое Kubernetes? Основные объекты.',
        answer: `**Kubernetes (K8s)** — оркестратор контейнеров: автоматизирует развёртывание, масштабирование, самовосстановление и обновление контейнеризованных приложений в кластере.

**Архитектура**: Control Plane (API Server, Scheduler, Controller Manager, etcd) + Worker Nodes (kubelet, контейнеры).

**Основные объекты:**

- **Pod** — минимальная единица: один или несколько контейнеров с общей сетью/хранилищем;
- **Deployment** — декларативное управление репликами Pod'ов: rolling update, rollback, масштабирование;
- **Service** — стабильная точка доступа к Pod'ам (балансировка): ClusterIP (внутри), NodePort, LoadBalancer;
- **Ingress** — HTTP-маршрутизация снаружи (домены, пути, TLS);
- **ConfigMap / Secret** — конфигурация и чувствительные данные отдельно от образа;
- **HorizontalPodAutoscaler** — автомасштабирование по CPU/метрикам;
- **StatefulSet** — для stateful-приложений (БД); **DaemonSet** — Pod на каждой ноде; **Job/CronJob** — разовые/периодические задачи;
- **PersistentVolume / PVC** — постоянное хранилище.

**Ключевые механизмы:**

- **декларативность**: описываете желаемое состояние (YAML), K8s его поддерживает (reconciliation loop);
- **самовосстановление**: упавший Pod пересоздаётся; liveness/readiness/startup **probes** управляют трафиком и рестартами;
- **rolling updates** — обновление без простоя;
- requests/limits — управление ресурсами.

Экосистема: Helm (пакеты/шаблоны манифестов), ArgoCD (GitOps), Prometheus + Grafana (мониторинг). Для Java важно: JVM должна уважать лимиты контейнера (\`-XX:MaxRAMPercentage\`, современные JDK делают это автоматически).`,
      },
      {
        id: 'service-boundaries',
        difficulty: 'medium',
        tags: ['basics', 'patterns'],
        related: ['monolith-vs-microservices'],
        question: 'Как определять границы микросервисов? Какие признаки слишком мелкого или крупного сервиса?',
        answer: `Границы выделяют **по бизнес-возможностям (business capabilities)**, а не по техническим слоям. Основной инструмент — **DDD**: сервис соответствует одному **bounded context** — области с единой моделью и языком. Хороший сервис владеет своими данными и меняется по одной бизнес-причине (высокая связность внутри, слабая связанность снаружи).

**Признаки слишком крупного сервиса:** он меняется по многим несвязанным причинам, его правит несколько команд, релиз затрагивает много несвязанной функциональности, внутри чётко видны независимые поддомены.

**Признаки слишком мелкого сервиса:** он почти не имеет собственной логики и постоянно «болтает» по сети с другими (chatty), любое изменение задевает сразу несколько сервисов (они всегда деплоятся вместе), возникают распределённые транзакции там, где могла быть локальная. Слишком мелкое дробление даёт «распределённый монолит» — минусы микросервисов без их плюсов.

Практика: начинать с более крупных сервисов и дробить по мере прояснения границ, а не наоборот.`,
      },
      {
        id: 'monolith-migration',
        difficulty: 'medium',
        tags: ['patterns'],
        related: ['monolith-vs-microservices'],
        question: 'Как подойти к миграции монолита на микросервисы?',
        answer: `Ключевой принцип — **не переписывать всё сразу** (big-bang рискован), а мигрировать инкрементально паттерном **Strangler Fig**: вокруг монолита постепенно «выращивают» микросервисы, перенаправляя на них отдельные функции, пока монолит не станет ненужным.

Типичный подход:

1. поставить перед монолитом **фасад/gateway**, чтобы перенаправлять трафик по частям;
2. выделять по одному **bounded context** — начиная с наименее связанных и наиболее ценных для отделения (высокая нагрузка, отдельный цикл релизов);
3. разделять данные: у нового сервиса — своя БД; синхронизацию решают через события или временный shared-доступ;
4. переключать трафик постепенно (можно канареечно), контролируя метрики;
5. удалять вырезанный код из монолита.

Важно заранее решить, **что не выделять**: сильно связанное ядро иногда дешевле оставить монолитом. Мигрируют, когда есть реальная боль (масштабирование, независимые релизы), а не ради моды.`,
      },
      {
        id: 'sync-vs-async',
        difficulty: 'medium',
        tags: ['communication'],
        related: ['microservices-communication'],
        question: 'Когда выбирать синхронное взаимодействие между сервисами, а когда асинхронное?',
        answer: `**Синхронное** (REST, gRPC) — вызывающий ждёт ответа здесь и сейчас.

- Плюсы: простота, немедленный результат, понятная отладка.
- Минусы: **временна́я связанность** — если вызываемый сервис недоступен или медленный, страдает вызывающий; цепочки синхронных вызовов усиливают отказы (каскад).
- Когда: нужен немедленный ответ (запрос данных для отображения), простые query-сценарии. gRPC — для быстрых внутренних вызовов, REST — для широкой совместимости.

**Асинхронное** (сообщения/события через Kafka, RabbitMQ) — отправитель публикует сообщение и не ждёт.

- Плюсы: **слабая связанность** во времени, устойчивость к недоступности получателя (сообщение подождёт в брокере), сглаживание пиков нагрузки, естественная событийная интеграция.
- Минусы: сложнее (итоговая согласованность, порядок, дубликаты, отладка распределённого потока).
- Когда: уведомления о событиях, длительные операции, развязывание сервисов, разные скорости обработки.

Правило: **команды/запросы с немедленным ответом — синхронно; факты о произошедшем (события) и развязка — асинхронно.** Часто в одной системе сочетают оба подхода.`,
      },
      {
        id: 'loose-coupling',
        difficulty: 'medium',
        tags: ['communication', 'patterns'],
        related: ['microservices-communication'],
        question: 'Что такое слабая связанность между микросервисами и как её обеспечить?',
        answer: `**Слабая связанность (loose coupling)** — сервисы можно менять и деплоить независимо, потому что они мало знают о внутреннем устройстве друг друга. Противоположность — «распределённый монолит», где сервисы приходится релизить вместе.

Как обеспечивают:

- **скрытие данных** — у каждого сервиса своя БД; нельзя лезть в чужую БД напрямую, только через API/события;
- **стабильные контракты** — общаются через версионируемые API/схемы событий; изменения делают обратно совместимыми (не ломая потребителей);
- **асинхронные события** вместо цепочек синхронных вызовов — развязка во времени;
- **отказ от общих моделей** — не делить общие доменные библиотеки/сущности между сервисами (общий код усиливает связанность);
- **толерантность к изменениям** — принцип «Tolerant Reader»: игнорировать незнакомые поля, не падать от несущественных изменений;
- изоляция сбоев (таймауты, circuit breaker), чтобы падение соседа не тянуло за собой.

Признак хорошей развязки: сервис можно задеплоить в проде, не координируя релиз с другими командами.`,
      },
      {
        id: 'database-per-service',
        difficulty: 'hard',
        tags: ['data', 'patterns'],
        related: ['ms-resilience'],
        question: 'Что такое «база данных на сервис» и как обеспечить согласованность данных?',
        answer: `**Database per service** — у каждого микросервиса собственная БД, к которой имеет доступ только он. Другие сервисы получают данные исключительно через его API или события. Это ключ к слабой связанности и независимому масштабированию, но убирает возможность одной ACID-транзакции и JOIN между сервисами.

**Согласованность** между сервисами становится **итоговой (eventual)**, а не мгновенной. Инструменты:

- **паттерн Saga** — распределённая бизнес-операция как цепочка локальных транзакций с компенсациями (хореография на событиях или оркестрация);
- **Transactional Outbox** — чтобы атомарно сохранить данные и опубликовать событие: событие пишется в ту же БД в таблицу outbox в одной транзакции, а отдельный процесс дочитывает её и шлёт в брокер (решает проблему «записал в БД, но не отправил событие»);
- **CQRS / реплики для чтения** — сервис держит у себя денормализованную копию нужных чужих данных, обновляемую по событиям, чтобы не ходить синхронно за каждым запросом;
- **идемпотентность** обработчиков — потому что доставка обычно at-least-once.

Проектируют так, чтобы бизнес допускал временну́ю несогласованность; где нужна строгая атомарность — это признак, что данные, возможно, должны жить в одном сервисе.`,
      },
      {
        id: 'ms-resilience',
        difficulty: 'hard',
        tags: ['resilience'],
        related: ['microservices-patterns', 'database-per-service'],
        question: 'Как обеспечить устойчивость к сбоям при взаимодействии микросервисов?',
        answer: `В распределённой системе сбои неизбежны, поэтому вызовы соседей всегда считают потенциально ненадёжными. Основные механизмы:

- **Timeout** — никогда не ждать ответа бесконечно; повисший вызов не должен держать ресурсы (потоки, соединения).
- **Retry + backoff** — повтор при транзиентных ошибках с экспоненциальной задержкой и jitter; только для **идемпотентных** операций, иначе усилит проблему.
- **Circuit Breaker** — после серии ошибок «размыкает» вызовы к упавшему сервису (fail fast + fallback), не давая каскаду и давая соседу восстановиться.
- **Bulkhead** — изоляция ресурсов (отдельные пулы потоков/соединений на разных вызываемых), чтобы отказ одного не выел все ресурсы приложения.
- **Fallback / graceful degradation** — запасной ответ (кэш, значение по умолчанию, урезанная функциональность) вместо полного отказа.
- **Rate limiting / throttling** — защита от перегрузки.

В экосистеме Spring это даёт **Resilience4j** (Circuit Breaker, Retry, Bulkhead, RateLimiter, TimeLimiter). Цель — чтобы отказ одного сервиса приводил к деградации, а не к падению всей системы. Подробнее о самих паттернах — в разделе «Шаблоны проектирования».`,
      },
      {
        id: 'ms-observability',
        difficulty: 'medium',
        tags: ['observability'],
        related: [],
        question: 'Как организовать мониторинг, логирование и трассировку микросервисов?',
        answer: `В распределённой системе поведение нельзя понять по одному сервису — нужны **три столпа observability**:

- **Логи** — структурированные (JSON), собираемые централизованно (например, **ELK/EFK**: Elasticsearch + Logstash/Fluentd + Kibana). Ключевое — **correlation id** запроса, прокидываемый через все сервисы, чтобы собрать сквозную картину.
- **Метрики** — числовые показатели во времени (RPS, latency p95/p99, ошибки, использование ресурсов). Обычно **Prometheus** (сбор) + **Grafana** (дашборды, алерты); в Spring — Micrometer + Actuator.
- **Трассировка (distributed tracing)** — путь одного запроса через все сервисы с таймингами каждого шага (**Jaeger**, **Zipkin**, OpenTelemetry). Показывает, где именно теряется время в цепочке вызовов.

Дополнительно: **health checks** (\`/actuator/health\`) для оркестратора, алерты на аномалии, SLO/SLI. Без этого отладка в микросервисах превращается в гадание — поэтому observability закладывают с самого начала, а не после инцидента.`,
      },
      {
        id: 'ms-security',
        difficulty: 'medium',
        tags: ['security'],
        related: [],
        question: 'Как реализуется аутентификация, авторизация и хранение секретов в микросервисах?',
        answer: `**Аутентификация и авторизация:**

- обычно **stateless** через токены: пользователь аутентифицируется, получает токен (часто **JWT**), который шлёт в каждый запрос — сервисы проверяют его без общей сессии;
- для делегированного доступа и внешних клиентов — **OAuth2 / OpenID Connect** с authorization server (выдаёт токены);
- **API Gateway** часто берёт на себя проверку токена на входе, а сервисы дополнительно проверяют права (роли/scopes) для своих операций;
- между сервисами — взаимная аутентификация, часто **mTLS** (обычно через service mesh), чтобы внутренний трафик тоже был доверенным (принцип zero-trust: не доверять сети по умолчанию).

**Секреты** (пароли БД, ключи, токены) не хранят в коде и не коммитят в git. Используют менеджеры секретов: **HashiCorp Vault**, **AWS Secrets Manager**, Kubernetes Secrets (+ шифрование). Они дают централизованное хранение, контроль доступа, аудит и **ротацию** секретов без пересборки сервисов.`,
      },
      {
        id: 'ms-cicd-testing',
        difficulty: 'medium',
        tags: ['deployment'],
        related: [],
        question: 'Как организовать CI/CD и тестирование микросервисов?',
        answer: `**CI/CD:** каждый сервис имеет **независимый пайплайн** и деплоится отдельно — это одно из главных преимуществ микросервисов. Пайплайн: сборка → тесты → упаковка в контейнер (Docker) → публикация образа → деплой в оркестратор (Kubernetes). Инструменты: Jenkins, GitLab CI, GitHub Actions; для деплоя в k8s — GitOps-подход (**ArgoCD**, Flux). Практики безопасного выката: blue-green и canary-релизы, автоматический откат по метрикам.

**Тестирование** строят пирамидой, с акцентом на распределённость:

- **юнит-тесты** — логика сервиса в изоляции (быстрые, их большинство);
- **интеграционные** — сервис с его БД/брокером; удобно поднимать реальные зависимости через **Testcontainers**;
- **контрактные тесты (contract testing)** — ключевые для микросервисов: проверяют, что API между потребителем и поставщиком совместимы (например, Spring Cloud Contract, Pact), не поднимая всю систему;
- **end-to-end** — сквозные сценарии через несколько сервисов; ценны, но медленные и хрупкие, поэтому их держат немного.

Идея: дорогих e2e-тестов — минимум, а совместимость сервисов проверять быстрыми контрактными тестами.`,
      },
      {
        id: 'api-gateway',
        difficulty: 'medium',
        tags: ['patterns'],
        related: ['microservices-communication', 'service-discovery'],
        question: 'Что такое API Gateway и зачем он нужен?',
        answer: `**API Gateway** — единая точка входа для внешних клиентов в микросервисную систему. Клиент обращается к шлюзу, а тот маршрутизирует запросы к нужным сервисам.

Зачем нужен:

- **единая точка входа** — клиент не знает адреса десятков сервисов и их изменения;
- **сквозные (cross-cutting) задачи** в одном месте: аутентификация/авторизация, rate limiting, CORS, TLS-терминация, логирование, кэширование;
- **маршрутизация и композиция** — направить запрос нужному сервису; иногда **агрегировать** несколько вызовов в один ответ;
- **развязка** внешнего API от внутренней декомпозиции сервисов (внутреннее дробление меняется, внешний контракт стабилен);
- **трансляция протоколов** — снаружи REST/graphQL, внутрь gRPC.

\`\`\`text
Клиент → [API Gateway] → Auth-сервис
                       → Orders-сервис
                       → Catalog-сервис
\`\`\`

Паттерн **BFF (Backend for Frontend)** — отдельный шлюз под каждый тип клиента (веб, мобильный).

Риски: шлюз может стать **узким местом** и единой точкой отказа — его делают stateless и масштабируют горизонтально; бизнес-логику в него не кладут (только маршрутизация и cross-cutting). Примеры: Spring Cloud Gateway, Kong, NGINX, AWS API Gateway.`,
      },
      {
        id: 'service-discovery',
        difficulty: 'medium',
        tags: ['patterns'],
        related: ['api-gateway', 'microservices-communication'],
        question: 'Что такое Service Discovery?',
        answer: `**Service Discovery** — механизм, позволяющий сервисам **находить сетевые адреса друг друга** динамически, без хардкода IP/портов. В облаке экземпляры поднимаются/гаснут и меняют адреса (автоскейлинг, рестарты), поэтому статическая конфигурация не работает.

Компоненты:

- **Service Registry** — реестр, где экземпляры **регистрируются** при старте и шлют heartbeat (Eureka, Consul, etcd, Zookeeper);
- **discovery** — потребитель запрашивает у реестра актуальные адреса и выбирает экземпляр.

Две модели:

- **client-side discovery** — клиент сам обращается к реестру и балансирует (напр. старый Netflix Eureka + Ribbon);
- **server-side discovery** — клиент идёт на балансировщик/шлюз, который смотрит в реестр (напр. Kubernetes Service + kube-dns/kube-proxy).

В **Kubernetes** discovery встроен: \`Service\` даёт стабильные DNS-имя и virtual IP, за которыми стоят меняющиеся \`Pod\`; поэтому отдельный Eureka часто не нужен.

Связка с балансировкой: получив список живых экземпляров, запросы распределяют между ними (round-robin и т.п.), а нездоровые исключают по health-checks.`,
      },
      {
        id: 'circuit-breaker',
        difficulty: 'hard',
        tags: ['patterns', 'resilience', 'advanced'],
        related: ['ms-resilience', 'service-discovery'],
        question: 'Что такое Circuit Breaker (предохранитель)?',
        answer: `**Circuit Breaker** — паттерн устойчивости, который **прекращает слать запросы к неисправной зависимости**, чтобы не копить зависшие вызовы и не ронять всю систему каскадно.

Аналогия — электрический предохранитель. Три состояния:

- **Closed** — запросы идут нормально; считаются ошибки. При превышении порога сбоев → переход в Open;
- **Open** — запросы **сразу отклоняются** (fail fast), не нагружая больной сервис; часто отдаётся **fallback** (кэш, заглушка). По таймауту → Half-Open;
- **Half-Open** — пропускается пробная порция запросов: успех → Closed, снова сбои → Open.

\`\`\`text
Closed ──(много ошибок)──▶ Open ──(таймаут)──▶ Half-Open ──(успех)──▶ Closed
                                                     └──(ошибка)──▶ Open
\`\`\`

Зачем: без предохранителя вызовы к упавшему сервису **копятся**, исчерпывают потоки/соединения вызывающего и роняют его тоже — **каскадный отказ**. Circuit breaker локализует сбой и даёт зависимости восстановиться.

Обычно сочетается с **timeout**, **retry (с backoff)**, **bulkhead** (изоляция пулов) и fallback. Реализации: Resilience4j (актуально), Netflix Hystrix (устарел), сервис-меш (Istio).`,
      },
      {
        id: 'saga-pattern',
        difficulty: 'hard',
        tags: ['patterns', 'advanced'],
        related: ['database-per-service', 'sync-vs-async'],
        question: 'Что такое паттерн Saga и как он обеспечивает согласованность?',
        answer: `**Saga** — паттерн управления **распределённой бизнес-транзакцией** между несколькими сервисами (у каждого своя БД), где обычная ACID-транзакция и 2PC неприменимы.

Идея: разбить операцию на последовательность **локальных транзакций**, каждая в своём сервисе. Если шаг падает, выполняются **компенсирующие транзакции**, отменяющие уже сделанные шаги (семантический откат, а не rollback).

\`\`\`text
Заказ:  создать заказ → списать оплату → зарезервировать товар → доставка
Сбой на резерве → компенсации: вернуть оплату → отменить заказ
\`\`\`

Два способа координации:

- **Хореография (choreography)** — сервисы реагируют на события друг друга без центрального дирижёра; проще, но логика размазана и труднее прослеживается;
- **Оркестрация (orchestration)** — центральный **оркестратор (saga orchestrator)** явно управляет шагами и компенсациями; логика в одном месте, но появляется координатор.

Особенности: согласованность **eventual** (не мгновенная); нужна **идемпотентность** шагов и компенсаций; компенсация не всегда возможна физически (письмо уже ушло) — тогда её проектируют семантически. Часто сочетается с **outbox** для надёжной публикации событий.`,
      },
      {
        id: 'cqrs',
        difficulty: 'hard',
        tags: ['patterns', 'advanced'],
        related: ['saga-pattern', 'database-per-service'],
        question: 'Что такое CQRS?',
        answer: `**CQRS (Command Query Responsibility Segregation)** — разделение модели на **команды** (изменяют состояние: create/update/delete) и **запросы** (только читают). Вместо одной модели «на всё» — отдельные модели записи и чтения.

\`\`\`text
Команды → Write-модель (нормализованная, инварианты) → БД записи
Запросы → Read-модель (денормализованная под запросы) → БД/проекции чтения
\`\`\`

Зачем:

- **независимое масштабирование** чтения и записи (обычно чтений кратно больше);
- **оптимизация под задачу** — write-модель бережёт инварианты (часто вместе с DDD-агрегатами), read-модель денормализована под конкретные экраны/отчёты, отвечает быстро;
- удобно совмещается с **event sourcing** и событийной интеграцией.

Синхронизация: read-модель обновляется из событий записи → она **eventually consistent** (может слегка отставать).

Важно: CQRS — **сложность не бесплатна** (две модели, синхронизация, отставание). Применять точечно, в подсистемах с высокой нагрузкой на чтение или сложными отчётами, а **не** повсеместно. Для простого CRUD — избыточен.`,
      },
      {
        id: 'modular-monolith',
        difficulty: 'medium',
        tags: ['architecture'],
        related: ['monolith-vs-microservices', 'loose-coupling', 'modular-monolith-first'],
        question: 'Что такое модульный монолит и чем он отличается от монолита «с пакетами»?',
        answer: `**Модульный монолит (modular monolith)** — единое разворачиваемое приложение (один процесс, один деплой), но **внутренне разбитое на модули с явными, соблюдаемыми границами**. Ключевое слово — «соблюдаемыми».

Обычный монолит «с пакетами» тоже поделён на пакеты, но границы **декоративные**: любой класс может обратиться к любому, и со временем всё связывается со всем («большой ком грязи»).

Модульный монолит отличается тем, что границы **реальны**:

- **явные границы модулей** — модуль = bounded context, а не техслой;
- **ограниченные зависимости** — модуль виден другим только через **публичный контракт (API/порт)**; внутренние классы инкапсулированы;
- **чёткая ответственность и владение** данными — модуль владеет своими таблицами, чужой не лезет в них напрямую;
- **никакого произвольного кросс-модульного доступа** — запрещено «тянуть» внутренности соседа (проверяется ArchUnit).

\`\`\`text
Монолит с пакетами:  границы есть на бумаге, любой класс → любой класс
Модульный монолит:   границы соблюдаются, модуль → только API соседа
\`\`\`

Итог: та же простота эксплуатации, что у монолита, но с дисциплиной границ, как у микросервисов — и с возможностью потом извлечь модуль в сервис.`,
      },
      {
        id: 'modular-monolith-first',
        difficulty: 'medium',
        tags: ['architecture'],
        related: ['modular-monolith', 'monolith-vs-microservices', 'when-extract-microservice'],
        question: 'Почему часто стоит начинать с модульного монолита, а не с микросервисов?',
        answer: `Начинать с микросервисов на раннем этапе — типичная **преждевременная сложность**. Модульный монолит обычно разумнее, потому что:

- **домен ещё меняется** — границы контекстов пока не устоялись; провести их в коде (модули) **дёшево**, а между сервисами (сеть, API, деплой) — дорого и трудно переигрывать;
- **сначала стабилизировать границы** — модули позволяют двигать границы рефакторингом, пока не станет ясно, где они верны;
- **ниже операционные издержки** — один деплой, одна БД, нет распределённых сбоев, ретраев, трассировки, версионирования контрактов;
- **проще рефакторинг, тесты и отладка** — всё в одном процессе, нет сетевых границ;
- **извлечение — потом, через контракты** — когда модуль созрел и есть операционное обоснование, его выносят в сервис по уже готовому контракту.

Это подход **«monolith first»**: получить преимущества чётких границ (как у микросервисов) без их операционной цены, и распределять систему **эволюционно**, под конкретным давлением (масштабирование, автономия команд), а не догматически с самого начала.`,
      },
      {
        id: 'modular-monolith-vs-microservices',
        difficulty: 'hard',
        tags: ['architecture', 'advanced'],
        related: ['modular-monolith', 'microservices-harder', 'monolith-vs-microservices'],
        question: 'Каковы trade-offs между модульным монолитом и микросервисами?',
        answer: `Это не «хорошо/плохо», а **компромисс**; модульный монолит — часто разумная середина.

**Модульный монолит:**

- ✓ простая эксплуатация (один деплой), простая отладка, **низкая задержка** (вызовы в процессе), одна транзакция/БД — сильная согласованность легко;
- ✓ лёгкий рефакторинг границ;
- ✗ **единый деплой** — нельзя выкатывать/масштабировать модули независимо;
- ✗ общий процесс — сбой/утечка в одном модуле влияет на все; ограничен одним стеком.

**Микросервисы:**

- ✓ **независимые** деплой и масштабирование, автономия команд, изоляция отказов, свобода технологий;
- ✗ **распределённая сложность**: сетевые сбои, ретраи, таймауты, дубли доставки, **eventual consistency**;
- ✗ нужны observability/трассировка, версионирование контрактов, оркестрация, больше операционных затрат.

Правило выбора: **модульный монолит** — пока домен молод, команда невелика, нужна простота и сильная согласованность; **микросервисы** — когда появляется реальное давление (независимое масштабирование, автономия команд, разный темп релизов). Часто путь — модульный монолит → извлечение отдельных сервисов по мере надобности.`,
      },
      {
        id: 'when-extract-microservice',
        difficulty: 'hard',
        tags: ['architecture', 'advanced'],
        related: ['modular-monolith-first', 'service-boundaries', 'prepare-monolith-extraction'],
        question: 'Когда извлекать модуль в отдельный микросервис?',
        answer: `Извлечение оправдано **конкретным давлением**, а не догмой «всё должно быть сервисами». Реальные причины:

- **независимое масштабирование** — модуль нагружен иначе, чем остальные, и его выгодно масштабировать отдельно;
- **отдельный темп деплоя** — модуль меняется часто и его хочется выкатывать независимо, не трогая остальное;
- **владение командой** — за модуль отвечает отдельная команда, которой нужна автономия релизов;
- **чёткая доменная автономия** — граница контекста устоялась, интеграция с остальным минимальна и идёт через контракт;
- **операционное обоснование** — разные требования к доступности/изоляции отказов/безопасности/ресурсам.

Предпосылка: модуль уже имеет **чистую границу и контракт** (в модульном монолите). Тогда извлечение — механическая замена внутрипроцессного вызова на REST/сообщения.

Анти-сигналы (извлекать **рано**): «модно», «микросервисы = современно», желание независимого деплоя при **сильной** связанности с остальными (получите распределённый монолит — худшее из обоих миров). Нет давления — нет причины платить распределённую цену.`,
      },
      {
        id: 'prepare-monolith-extraction',
        difficulty: 'hard',
        tags: ['architecture', 'advanced'],
        related: ['modular-monolith', 'monolith-migration', 'when-extract-microservice'],
        question: 'Как подготовить монолит к будущему извлечению сервисов без оверинжиниринга?',
        answer: `Идея — **проектировать на вырост через границы, а не через инфраструктуру**. Не нужно заранее строить Kafka и Kubernetes; нужно, чтобы модули были **готовы отделиться**:

- **выделить bounded contexts** — понять и зафиксировать доменные границы (см. модульный монолит);
- **изолировать модули** — каждый контекст в своём модуле с инкапсулированными внутренностями;
- **общение через интерфейсы/контракты** — модули зависят от API соседа, а не от реализации; это будущая точка замены на REST/сообщения;
- **избегать прямой общей внутренней связанности** — никаких общих доменных объектов и походов в чужие таблицы;
- **разделить domain и integration** — интеграционный код за отдельным слоем (ACL);
- **чёткое владение данными и правилами** — у каждого модуля своя схема/таблицы; кросс-доступ только через API (готовит «базу на сервис»).

Ключ — **без оверинжиниринга**: пока это один процесс и одна БД (просто с логически разделёнными схемами), синхронные вызовы через интерфейсы. Инфраструктуру распределённости добавляют **только при фактическом извлечении**. Тогда вынос модуля = замена локальной реализации контракта на удалённую.`,
      },
      {
        id: 'microservices-harder',
        difficulty: 'medium',
        tags: ['architecture'],
        related: ['modular-monolith-vs-microservices', 'ms-resilience', 'ms-observability'],
        question: 'Что становится сложнее после перехода на микросервисы?',
        answer: `Распределённость добавляет целый пласт сложности, которого не было в одном процессе:

- **сетевые сбои** — вызов может не дойти/зависнуть; сеть ненадёжна по определению;
- **ретраи и таймауты** — нужны на каждый удалённый вызов (с backoff), иначе каскадные отказы;
- **дубли доставки** — доставка обычно at-least-once → обязательна **идемпотентность**;
- **eventual consistency** — нет единой транзакции между сервисами; согласованность через события (saga, outbox);
- **распределённая трассировка** — поток запроса размазан по сервисам; нужны correlation-id, tracing (Jaeger/Zipkin), агрегированные логи;
- **версионирование контрактов** — API меняются независимо; нужна обратная совместимость и контрактные тесты;
- **операционные издержки** — деплой десятков сервисов, service discovery, конфигурация, секреты, мониторинг, схемы сообщений.

Плюс труднее **отладка** (нет единого стектрейса) и **локальный запуск** (поднять много сервисов). Именно поэтому распределяют по необходимости: за автономию и масштабируемость платят этой сложностью.`,
      },
      {
        id: 'context-not-always-service',
        difficulty: 'medium',
        tags: ['architecture'],
        related: ['modular-monolith', 'service-boundaries'],
        question: 'Каждый ли Bounded Context — это отдельный микросервис?',
        answer: `**Нет.** Bounded Context — это **логическая** граница модели, языка и ответственности. Микросервис — это **физическое** решение о деплое. Это два **разных** решения, которые не обязаны совпадать один-к-одному.

- один Bounded Context **может** быть отдельным сервисом — но не обязан;
- **несколько** контекстов могут жить в одном разворачиваемом юните (например, в модульном монолите — по модулю на контекст, но один деплой);
- иногда контекст оказывается слишком мелким, чтобы оправдать отдельный сервис (лишние сеть и операционные издержки).

Правильный порядок: **сначала** выделить логические границы (bounded contexts), **потом** отдельно решать, как их разворачивать — вместе или врозь — исходя из масштабирования, автономии команд, темпа релизов.

Формула «один контекст = один сервис» — распространённое **упрощение DDD**, ведущее либо к слишком дробным сервисам (распределённая сложность без пользы), либо к натягиванию границ под инфраструктуру. Логическая декомпозиция и физическое развёртывание — независимые оси.`,
      },
      {
        id: 'decompose-evolving',
        difficulty: 'hard',
        tags: ['architecture', 'advanced'],
        related: ['modular-monolith-first', 'good-decomposition-criteria', 'ddd/identify-bounded-contexts'],
        question: 'Как декомпозировать систему, когда требования ещё меняются?',
        answer: `В условиях неопределённости главное — **не зафиксировать дорогие границы слишком рано**. Подход:

- **начинать от business capabilities** — делить по бизнес-возможностям/ответственности, а не по таблицам, CRUD-сущностям или экранам UI;
- **задать границы и ответственность** — что модуль делает и, важно, **что вне его области** (out-of-scope);
- **держать контракты чистыми** — общение между частями через явные интерфейсы, чтобы границу можно было двигать без переписывания всех;
- **избегать преждевременного распределения** — пока границы гибкие, держать их **в коде** (модульный монолит), а не в сети; двигать модули дёшево, сервисы — дорого;
- **эволюционировать под давлением** — распределять/дробить там, где появилась реальная боль (масштаб, автономия, темп релизов), а не «на всякий случай».

Суть: сначала **дешёвые, подвижные** границы (модули + контракты), стабилизация языка и ответственности, и только затем — фиксация и, при необходимости, вынесение в сервисы. Проектирование границ — итеративный процесс, а не разовое решение на старте.`,
      },
      {
        id: 'good-decomposition-criteria',
        difficulty: 'hard',
        tags: ['architecture', 'advanced'],
        related: ['decompose-evolving', 'decomposition-mistakes', 'loose-coupling'],
        question: 'Как понять, что декомпозиция системы хорошая?',
        answer: `У хорошей декомпозиции есть **проверяемые критерии**, а не просто «нравится»:

- **чёткая ответственность** — про каждый модуль/сервис можно в одном предложении сказать, за что он отвечает;
- **явная область вне ответственности (out-of-scope)** — понятно не только что он делает, но и чего **не** делает;
- **разные причины изменений разведены** — части, меняющиеся по независимым бизнес-поводам, лежат в разных границах (когезия по причине изменения);
- **приемлемая связанность (coupling)** — мало межмодульных зависимостей, они идут через контракты и в одном направлении, без циклов; нет «болтливой» интеграции;
- **понятный язык** — внутри границы термины однозначны (ubiquitous language не «ломается»);
- **эволюция даётся легче** — типичное изменение затрагивает **один** модуль, а не расползается по многим; границу можно двигать/извлекать без переписывания всего.

Плохие признаки-антиподы: изменение одной фичи трогает половину модулей, постоянные синхронные хождения между границами, общие данные, «shotgun surgery». Хорошая декомпозиция локализует изменения и делает систему **понятной и развиваемой**.`,
      },
      {
        id: 'decomposition-mistakes',
        difficulty: 'medium',
        tags: ['architecture'],
        related: ['good-decomposition-criteria', 'service-boundaries', 'context-not-always-service'],
        question: 'Какие типичные ошибки при выделении доменов и сервисов?',
        answer: `Частые архитектурные ошибки декомпозиции:

- **деление по таблицам БД** — сервис/модуль на таблицу; границы получаются техническими, а не бизнесовыми, и всё связано джойнами/вызовами;
- **деление по CRUD-сущностям** — «UserService», «OrderService» как обёртки над сущностями вместо бизнес-возможностей; логика размазывается между ними;
- **деление по экранам UI** — граница повторяет интерфейс, а не домен; при смене UI всё рушится;
- **смешивание разных контекстов** — например, profile (кто ты) и access (что тебе можно) в одном; разные причины изменений слипаются;
- **слишком крупные контексты** — «god service», внутри которого снова каша; или **слишком мелкие** — распределённая сложность без пользы, болтливая интеграция;
- **раннее извлечение микросервисов** — распределение до стабилизации границ → распределённый монолит (жёсткая связанность + сетевые издержки, худшее из миров).

Общий корень: делить по **технической структуре** (таблицы/CRUD/экраны), а не по **бизнес-возможностям и языку**. Правильно — от capabilities, инвариантов и причин изменений, а физическое развёртывание решать отдельно.`,
      },
      {
        id: 'evolve-inprocess-to-rest-kafka',
        difficulty: 'hard',
        tags: ['architecture', 'advanced'],
        related: ['prepare-monolith-extraction', 'sync-vs-async', 'microservices-communication'],
        question: 'Как эволюционировать внутрипроцессное взаимодействие модулей в REST или Kafka?',
        answer: `Эволюция опирается на то, что модули уже общаются **через интерфейсы (контракты)** — тогда меняется лишь реализация вызова, а не вызывающий код.

Порядок:

1. **начать с интерфейсов** — в модульном монолите модуль зависит от порта соседа, а не от реализации; это точка будущей замены;
2. **разделить sync и async потребности** — где нужен немедленный ответ (запрос/проверка) → синхронный вызов; где достаточно узнать о факте позже (уведомить, распространить изменение) → асинхронно;
3. **заменить локальную реализацию на REST** для **запросов/проверок** (query, «дай данные», «проверь право») — синхронный вызов по контракту; добавить таймауты, ретраи, circuit breaker;
4. **использовать Kafka для async-распространения/событий** — вместо прямого вызова модуль **публикует доменное событие**, подписчики реагируют; развязка и устойчивость к недоступности;
5. **осознать новые trade-offs** — появились сетевые сбои, eventual consistency, дубли (→ идемпотентность), версионирование контрактов, трассировка; надёжную публикацию событий после изменения БД обеспечивает **outbox**.

Суть: чистый контракт превращает переход «внутрипроцессно → REST/Kafka» из переписывания в **замену реализации** — но осознанно принимая распределённую цену там, где она оправдана.`,
      },
    ],
  };
