// Категория: event-driven
export const eventDriven = {
  id: 'event-driven',
  title: 'Event-Driven Architecture',
  icon: '📨',
  description: 'Событийная архитектура, семантики доставки, идемпотентность, DLQ, outbox/inbox',
  questions: [
    {
      id: 'eventual-consistency',
      difficulty: 'medium',
      tags: ['basics', 'consistency'],
      related: ['eda-benefits', 'eda-drawbacks'],
      question: 'Что такое eventual consistency (конечная согласованность)?',
      answer: `**Eventual consistency** — модель согласованности, при которой после прекращения изменений все реплики/сервисы **со временем** придут к одинаковому состоянию, но в **какой-то момент** могут его не иметь (видят устаревшие данные).

Противопоставляется **strong consistency**, где любое чтение сразу видит последнюю запись.

В событийных системах это естественно: сервис A меняет своё состояние и публикует событие; сервис B обработает его чуть позже — между этими моментами данные рассогласованы.

Почему приходится мириться:

- по **теореме CAP** в распределённой системе при сетевом разделении нельзя одновременно иметь и сильную согласованность, и доступность — часто выбирают доступность + eventual;
- атомарные распределённые транзакции (2PC) дороги и плохо масштабируются.

Практика: бизнес проектируют так, чтобы он допускал краткое рассогласование («заказ принят, письмо придёт через секунду»), а согласованность достигается через события, ретраи и идемпотентность.`,
    },
    {
      id: 'eda-benefits',
      difficulty: 'medium',
      tags: ['basics'],
      related: ['eda-drawbacks', 'eventual-consistency'],
      question: 'Каковы основные преимущества событийной архитектуры?',
      answer: `**Event-Driven Architecture (EDA)** строит взаимодействие вокруг асинхронных событий («что произошло»), а не прямых команд-вызовов. Плюсы:

- **Слабая связанность (loose coupling)** — издатель не знает о потребителях; новых подписчиков добавляют, не меняя источник;
- **Масштабируемость и устойчивость к всплескам** — брокер сглаживает нагрузку (буферизация), потребители масштабируются независимо;
- **Отказоустойчивость** — если потребитель недоступен, события ждут в брокере; после восстановления он их дочитает (нет каскадного отказа, как при синхронном вызове);
- **Расширяемость** — на одно событие независимо реагируют несколько подсистем (аналитика, уведомления, аудит);
- **Аудит и воспроизводимость** — лог событий (особенно в Kafka) даёт историю и возможность переиграть (event sourcing, пересборка проекций).

EDA хорошо ложится на микросервисы: реакция на изменения без синхронной зависимости от чужой доступности.`,
    },
    {
      id: 'eda-drawbacks',
      difficulty: 'hard',
      tags: ['advanced'],
      related: ['eda-benefits', 'eventual-consistency', 'idempotency-importance'],
      question: 'Каковы основные недостатки событийной архитектуры?',
      answer: `За развязку приходится платить сложностью:

- **Eventual consistency** — нет мгновенной согласованности; бизнес и UI должны это учитывать;
- **Сложность отладки и трассировки** — поток управления неявный, размазан по подписчикам; нужны correlation-id и распределённая трассировка;
- **Дубли и переупорядочивание** — доставка обычно at-least-once, порядок гарантирован лишь частично → обязательна **идемпотентность** потребителей;
- **Труднее рассуждать о состоянии** — «истина» распределена по событиям и проекциям;
- **Обработка ошибок нетривиальна** — нужны ретраи, backoff, **DLQ**, мониторинг лагов;
- **Схемы событий эволюционируют** — нужна версияция и совместимость (schema registry);
- **Риск «событийного монолита»** — при плохих границах события связывают сервисы не слабее прямых вызовов.

Вывод: EDA — мощный инструмент, но добавляет операционную и когнитивную сложность; применять по необходимости, а не по умолчанию.`,
    },
    {
      id: 'at-least-once',
      difficulty: 'medium',
      tags: ['delivery'],
      related: ['at-most-once', 'exactly-once', 'idempotency-importance'],
      question: 'Что такое доставка at-least-once (хотя бы один раз)?',
      answer: `**At-least-once** — гарантия, что сообщение будет доставлено **как минимум один раз**, но возможны **повторы (дубликаты)**.

Как возникает: потребитель обрабатывает сообщение, но **падает или теряет сеть до подтверждения (ack/commit offset)**. Брокер не увидел подтверждения и доставит сообщение снова.

\`\`\`text
receive → process → (crash до commit) → redelivery → process ещё раз
\`\`\`

- **Плюс**: не теряем сообщения — подходит для большинства бизнес-задач;
- **Минус**: возможны дубли → потребитель **обязан быть идемпотентным**;
- это **режим по умолчанию** в большинстве брокеров (Kafka с ручным/периодическим коммитом offset, RabbitMQ с ack).

At-least-once + идемпотентность на практике дают эффект «как будто ровно один раз» без дорогой распределённой координации.`,
    },
    {
      id: 'at-most-once',
      difficulty: 'medium',
      tags: ['delivery'],
      related: ['at-least-once', 'exactly-once'],
      question: 'Что такое доставка at-most-once (не более одного раза)?',
      answer: `**At-most-once** — сообщение доставляется **не более одного раза**: дубликатов нет, но при сбое сообщение может быть **потеряно**.

Как возникает: потребитель **подтверждает/коммитит offset до обработки** (или fire-and-forget). Если он упадёт после подтверждения, но до обработки — сообщение уже считается «прочитанным» и не будет доставлено снова.

\`\`\`text
receive → commit offset → (crash до process) → сообщение потеряно
\`\`\`

- **Плюс**: минимальные накладные расходы, нет дублей, самая высокая скорость;
- **Минус**: возможна потеря — недопустимо там, где важно каждое сообщение (платежи, заказы);
- уместно для **некритичных потоков**: метрики, телеметрия, «best-effort» уведомления, где потеря отдельных точек не страшна.

Выбор at-most-once vs at-least-once — компромисс «потеря против дублей».`,
    },
    {
      id: 'exactly-once',
      difficulty: 'hard',
      tags: ['delivery', 'advanced'],
      related: ['at-least-once', 'at-most-once', 'idempotent-consumers'],
      question: 'Что такое доставка exactly-once (ровно один раз)?',
      answer: `**Exactly-once** — каждое сообщение учитывается **ровно один раз**: ни потерь, ни дублей. Самая сильная и самая дорогая гарантия.

Важный нюанс: **абсолютной** exactly-once **доставки** в распределённой системе не бывает (сеть может дублировать/терять). Достигается **exactly-once обработка (processing)** — комбинацией:

- **at-least-once доставки** + **идемпотентности** потребителя (дедупликация по ключу/offset), либо
- **транзакций**, атомарно связывающих чтение, обработку и запись результата/offset.

В Kafka это **транзакции + идемпотентный продюсер** (EOS): \`read-process-write\` внутри одной транзакции, \`isolation.level=read_committed\` у читателя. Работает, пока всё в пределах Kafka; при записи во внешнюю БД нужна дополнительная идемпотентность (например, outbox/inbox).

Правило: exactly-once дорог; чаще выбирают **at-least-once + идемпотентность**, что даёт тот же наблюдаемый эффект дешевле.`,
    },
    {
      id: 'idempotency-importance',
      difficulty: 'medium',
      tags: ['idempotency'],
      related: ['at-least-once', 'idempotent-consumers', 'exactly-once'],
      question: 'Почему идемпотентность важна в системах на сообщениях?',
      answer: `**Идемпотентность** — свойство операции, при котором её **повторное применение с теми же данными не меняет результат** по сравнению с однократным.

Почему критична: реальная доставка почти всегда **at-least-once**, значит потребитель **неизбежно** получит некоторые сообщения повторно (после ретраев, ребаланса, сбоев). Без идемпотентности это приводит к двойным списаниям, дублям заказов, повторным письмам.

\`\`\`text
«Списать 100₽ со счёта»      — НЕ идемпотентно (два раза спишет 200₽)
«Установить статус = PAID»    — идемпотентно (повтор ничего не меняет)
«Провести платёж с id=X»      — можно сделать идемпотентным через дедуп по id
\`\`\`

Идемпотентность **дешевле и надёжнее**, чем попытка гарантировать exactly-once доставку: она делает повтор безопасным, поэтому ретраи и at-least-once перестают быть проблемой. Это фундамент устойчивой обработки сообщений.`,
    },
    {
      id: 'idempotent-consumers',
      difficulty: 'hard',
      tags: ['idempotency', 'advanced'],
      related: ['idempotency-importance', 'inbox-pattern'],
      question: 'Как проектировать идемпотентных потребителей?',
      answer: `Цель — сделать повторную обработку одного и того же сообщения **безопасной**. Основные приёмы:

- **Дедупликация по идентификатору** — у каждого сообщения устойчивый \`messageId\`/бизнес-ключ; храним обработанные id и пропускаем повтор (**inbox-паттерн**):

\`\`\`sql
INSERT INTO processed_messages(message_id) VALUES (?)
ON CONFLICT DO NOTHING;   -- если уже есть — сообщение уже обработано
\`\`\`

- **Естественная идемпотентность операции** — формулировать как «установить состояние», а не «изменить на дельту» (upsert, \`status = PAID\` вместо \`balance -= 100\`);
- **Оптимистичная блокировка / версии** — применять изменение только если версия совпадает;
- **Атомарность** «пометить обработанным + записать результат» — в одной транзакции БД, иначе возможен разрыв;
- **Дедуп-ключ с TTL** (Redis) для потоков, где хранить все id накладно.

Практика: at-least-once брокер + идемпотентный потребитель = наблюдаемый эффект exactly-once без распределённых транзакций.`,
    },
    {
      id: 'dead-letter-queue',
      difficulty: 'medium',
      tags: ['errors'],
      related: ['dlq-when', 'retry-vs-dlq'],
      question: 'Что такое dead-letter queue (очередь недоставленных сообщений)?',
      answer: `**Dead-Letter Queue (DLQ)** — отдельная очередь/топик, куда отправляют сообщения, которые **не удалось обработать** (после исчерпания ретраев) или которые нельзя доставить/принять.

Идея: не терять «ядовитые» (poison) сообщения и не блокировать ими основной поток. Проблемное сообщение уводится в сторону, обработка остальных продолжается.

Что попадает в DLQ:

- бизнес-/десериализационная ошибка, которую ретраи не исправят (битый формат, невалидные данные);
- превышен лимит попыток обработки;
- истёк TTL сообщения или переполнена очередь (в брокерах с DLX).

С DLQ дальше работают: **алертинг**, разбор причины, ручная правка и **повторная подача (redrive)** после исправления. В RabbitMQ реализуется через dead-letter-exchange; в Kafka — обычно отдельным dead-letter-топиком (например, средствами Spring Kafka / Kafka Connect).`,
    },
    {
      id: 'dlq-when',
      difficulty: 'medium',
      tags: ['errors'],
      related: ['dead-letter-queue', 'retry-vs-dlq', 'exponential-backoff'],
      question: 'Когда сообщение должно попадать в DLQ?',
      answer: `Сообщение уводят в DLQ, когда дальнейшие попытки обработать его в основном потоке **бессмысленны или вредны**:

- **исчерпаны ретраи** — превышено максимальное число попыток (обычно с exponential backoff);
- **неустранимая (non-transient) ошибка** — данные невалидны, схема несовместима, десериализация падает: повтор не поможет;
- **poison message** — блокирует партицию/очередь и мешает обрабатывать остальные;
- реже — **истёк TTL** или переполнение очереди.

Важно **различать типы ошибок**:

- **transient** (временная: таймаут, недоступность БД) → **ретраить** с backoff, в DLQ **не** отправлять сразу;
- **permanent** (постоянная: битые данные, бизнес-нарушение) → сразу или после нескольких попыток в DLQ.

Отправка всего подряд в DLQ при временных сбоях — антипаттерн: наводняет DLQ и прячет реальную проблему доступности.`,
    },
    {
      id: 'retry-vs-dlq',
      difficulty: 'medium',
      tags: ['errors'],
      related: ['dead-letter-queue', 'dlq-when', 'exponential-backoff'],
      question: 'В чём разница между ретраями и обработкой через DLQ?',
      answer: `Это **две стадии** стратегии обработки ошибок, а не альтернативы:

**Retry (повтор)** — попытка обработать то же сообщение снова, рассчитанная на **временные (transient)** сбои: сеть моргнула, БД недоступна секунду, сработал rate limit. Обычно с **exponential backoff** и джиттером, ограниченным числом попыток. Часто повтор делают асинхронно (retry-топики/очереди с задержкой), чтобы не блокировать поток.

**DLQ** — «терминал» для сообщений, которые **ретраи не спасут** (исчерпан лимит или ошибка постоянная). Их убирают из основного потока для последующего разбора и повторной подачи вручную.

\`\`\`text
process → fail → retry (backoff, N раз) → всё ещё fail → DLQ → алерт/разбор
\`\`\`

Кратко: **ретраи** борются с временными сбоями автоматически; **DLQ** изолирует то, что автоматически не чинится, не давая ему блокировать систему и не теряя данные.`,
    },
    {
      id: 'exponential-backoff',
      difficulty: 'medium',
      tags: ['errors', 'resilience'],
      related: ['retry-vs-dlq', 'dlq-when'],
      question: 'Что такое экспоненциальный backoff?',
      answer: `**Exponential backoff** — стратегия повторов, при которой пауза между попытками **растёт экспоненциально**: например 1с, 2с, 4с, 8с… (\`base * 2^attempt\`), обычно с верхним потолком.

Зачем: при перегрузке или сбое зависимого сервиса частые немедленные ретраи только **усугубляют** проблему (retry storm). Растущие паузы дают сервису время восстановиться.

**Jitter (случайное дрожание)** — к задержке добавляют случайную составляющую, чтобы множество клиентов, упавших одновременно, не ретраили **синхронно** (thundering herd) и не били в сервис пачками.

\`\`\`text
delay = min(cap, base * 2^attempt)      // экспонента с потолком
delay = random(0, delay)                // full jitter
\`\`\`

Дополняют:

- **ограничение числа попыток** → затем DLQ;
- **circuit breaker** — вовсе перестать долбить неисправную зависимость на время.

Backoff + jitter + лимит попыток — стандарт устойчивых распределённых систем.`,
    },
    {
      id: 'outbox-pattern',
      difficulty: 'hard',
      tags: ['patterns', 'advanced'],
      related: ['outbox-why', 'reliable-publishing', 'inbox-pattern'],
      question: 'Что такое паттерн Outbox (транзакционный ящик исходящих)?',
      answer: `**Transactional Outbox** решает проблему **двойной записи (dual write)**: как атомарно и изменить БД, и опубликовать событие, если это два разных ресурса (БД и брокер) без общей транзакции.

Идея: событие пишется **в ту же БД, в той же транзакции**, что и бизнес-данные — в специальную таблицу \`outbox\`:

\`\`\`sql
BEGIN;
  INSERT INTO orders(...) VALUES (...);
  INSERT INTO outbox(id, type, payload) VALUES (...);   -- та же транзакция
COMMIT;
\`\`\`

Отдельный процесс (**relay**) затем читает \`outbox\` и публикует события в брокер:

- **polling publisher** — периодически выбирает неотправленные строки и шлёт в Kafka/RabbitMQ, помечая отправленными;
- **CDC** (Change Data Capture, напр. Debezium) — читает WAL/binlog и стримит изменения outbox без опроса.

Так «изменил данные» и «есть событие для отправки» становятся **атомарными** (одна транзакция БД), а публикация — надёжной и повторяемой.`,
    },
    {
      id: 'outbox-why',
      difficulty: 'hard',
      tags: ['patterns', 'advanced'],
      related: ['outbox-pattern', 'reliable-publishing'],
      question: 'Чем полезен паттерн Outbox?',
      answer: `Outbox устраняет **гонку двойной записи** и её тяжёлые последствия. Без него возможны два сценария рассогласования:

- «сохранили заказ, но упали **до** публикации события» → данные есть, потребители о них не узнали;
- «опубликовали событие, но транзакция с данными **откатилась**» → потребители реагируют на то, чего в БД нет.

Что даёт Outbox:

- **атомарность** бизнес-изменения и факта «событие нужно отправить» — обе записи в одной транзакции БД;
- **надёжная доставка at-least-once** — relay повторяет публикацию, пока не подтвердит брокер (отсюда — обязательная идемпотентность потребителей);
- **без распределённых транзакций (2PC)** — используется только локальная транзакция БД, что дёшево и масштабируемо;
- **упорядоченность и аудит** — outbox хранит историю исходящих.

По сути Outbox превращает ненадёжную операцию «БД + брокер» в надёжную «одна БД + асинхронная досылка».`,
    },
    {
      id: 'inbox-pattern',
      difficulty: 'hard',
      tags: ['patterns', 'advanced'],
      related: ['idempotent-consumers', 'outbox-pattern'],
      question: 'Что такое паттерн Inbox (ящик входящих)?',
      answer: `**Inbox** — зеркальный к Outbox паттерн на стороне **потребителя**, обеспечивающий **идемпотентную обработку** входящих сообщений и защиту от дублей (неизбежных при at-least-once).

Идея: перед обработкой сообщение регистрируется в таблице \`inbox\` по устойчивому \`messageId\`; в той же транзакции применяется бизнес-эффект:

\`\`\`sql
BEGIN;
  INSERT INTO inbox(message_id) VALUES (?)   -- уникальный ключ
    ON CONFLICT DO NOTHING;                  -- дубликат → пропускаем
  -- если строка вставилась (сообщение новое) — применяем бизнес-логику
COMMIT;
\`\`\`

Свойства:

- **дедупликация** — повторное сообщение с тем же id не применяется второй раз;
- **атомарность** «зафиксировали факт обработки + применили эффект» — одна транзакция БД, без разрыва;
- часто идёт **в паре с Outbox**: inbox гарантирует «обработать один раз», outbox — «надёжно опубликовать результат».

Inbox — конкретная реализация идемпотентного потребителя через персистентную дедупликацию.`,
    },
    {
      id: 'reliable-publishing',
      difficulty: 'hard',
      tags: ['patterns', 'advanced'],
      related: ['outbox-pattern', 'outbox-why', 'eventual-consistency'],
      question: 'Как обеспечить надёжную публикацию событий после изменения БД?',
      answer: `Проблема: изменить БД и опубликовать событие в брокер — это запись в **два разных ресурса**. Без общей транзакции любой порядок даёт риск рассогласования (данные без события или событие без данных). Возможные решения:

1. **Transactional Outbox (рекомендуется)** — событие пишется в таблицу \`outbox\` в **той же транзакции**, что и данные; отдельный relay/CDC надёжно досылает его в брокер. Атомарно и без 2PC.
2. **Change Data Capture (Debezium)** — стримить сами изменения таблиц из WAL/binlog; частный случай — CDC поверх outbox-таблицы.
3. **Event sourcing** — источник истины — сам журнал событий; публикация естественно следует из добавления события.
4. **Распределённая транзакция (2PC)** — атомарна, но дорогая, плохо масштабируется и слабо поддержана брокерами → на практике избегают.

Чего **не** делать: «сохранить в БД, потом в отдельном шаге вызвать \`producer.send()\`» — классический dual-write, теряющий события при сбое между шагами.

Во всех рабочих вариантах доставка — **at-least-once**, поэтому потребители обязаны быть **идемпотентными** (inbox).`,
    },
  ],
};
