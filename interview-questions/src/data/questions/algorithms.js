// Категория: algorithms
// Оригинальный набор вопросов по алгоритмам и структурам данных для
// собеседования. Ответы — markdown; проставлены сложность, теги и связи (item 14).
export const algorithms = {
  id: 'algorithms',
  title: 'Алгоритмы и структуры данных',
  icon: '🧮',
  description: 'Сложность, структуры данных, поиск, сортировки, графы, DP',
  questions: [
    {
      id: 'alg-complexity',
      difficulty: 'easy',
      tags: ['basics'],
      related: ['alg-common-complexities', 'alg-binary-search'],
      question: 'Что такое временная и пространственная сложность (Big-O)?',
      answer: `**Big-O** описывает, как растёт число операций (время) или объём памяти (пространство) алгоритма при увеличении размера входа **n** — по **верхней асимптотической границе**, отбрасывая константы и младшие члены.

- **Временная сложность** — сколько операций выполняется как функция от n. \`O(2n + 5)\` упрощается до \`O(n)\`: константы и коэффициенты не важны при больших n.
- **Пространственная сложность** — сколько дополнительной памяти нужно (не считая входных данных). Рекурсия тратит память на стек вызовов.

Зачем: позволяет сравнивать алгоритмы **независимо от железа** и предсказывать поведение на больших данных. \`O(n)\` и \`O(n²)\` на 10 элементах почти неразличимы, но на миллионе — разница в миллион раз.

Рядом с O (худший случай) существуют **Ω** (нижняя граница, лучший случай) и **Θ** (точная граница). На собеседовании под «сложностью» обычно подразумевают именно O — **худший случай**.`,
    },
    {
      id: 'alg-common-complexities',
      difficulty: 'easy',
      tags: ['basics'],
      related: ['alg-complexity', 'alg-sorting'],
      question: 'Назовите распространённые классы сложности от лучшего к худшему.',
      answer: `От самого быстрого роста к самому медленному (лучше — выше):

- **O(1)** — константное. Доступ по индексу массива, вставка в хеш-таблицу (в среднем).
- **O(log n)** — логарифмическое. Каждый шаг вдвое сокращает пространство: бинарный поиск, операции в сбалансированном дереве.
- **O(n)** — линейное. Один проход по массиву.
- **O(n log n)** — «линейно-логарифмическое». Эффективные сортировки сравнением (merge, heap, quick в среднем). Практический предел для сортировки сравнением.
- **O(n²)** — квадратичное. Вложенные циклы: пузырьковая сортировка, наивная проверка всех пар.
- **O(2ⁿ)** — экспоненциальное. Перебор всех подмножеств, наивные рекурсивные решения (например, числа Фибоначчи без мемоизации).
- **O(n!)** — факториальное. Перебор всех перестановок (задача коммивояжёра «в лоб»).

Ориентир: \`O(1)\`/\`O(log n)\` — отлично; \`O(n)\`/\`O(n log n)\` — хорошо; \`O(n²)\` — приемлемо для небольших n; \`O(2ⁿ)\`/\`O(n!)\` — только для крошечных входов.`,
    },
    {
      id: 'alg-array-vs-linkedlist',
      difficulty: 'easy',
      tags: ['data-structures'],
      related: ['alg-hashtable', 'alg-stack-queue'],
      question: 'В чём разница между массивом и связным списком?',
      answer: `**Массив** — непрерывный блок памяти с элементами фиксированного размера.

- доступ по индексу — **O(1)** (адрес вычисляется арифметически);
- вставка/удаление в середину — **O(n)** (нужно сдвигать элементы);
- динамический массив (ArrayList) при переполнении удваивает ёмкость — амортизированно \`O(1)\` на добавление в конец;
- хорошая **локальность кэша** → быстрый последовательный обход.

**Связный список** — узлы, разбросанные в памяти, каждый хранит данные и ссылку на следующий (и, в двусвязном, на предыдущий).

- доступ по индексу — **O(n)** (нужно пройти от головы);
- вставка/удаление, **когда узел уже найден**, — **O(1)** (перецепить ссылки);
- тратит память на ссылки, плохая локальность кэша.

**Когда что:** массив — если нужен произвольный доступ и обход (большинство случаев); связный список — если много вставок/удалений по краям или в известной позиции и не нужен доступ по индексу (например, реализация очереди/дека).`,
    },
    {
      id: 'alg-hashtable',
      difficulty: 'medium',
      tags: ['data-structures', 'hashing'],
      related: ['alg-array-vs-linkedlist', 'alg-common-complexities'],
      question: 'Как устроена хеш-таблица и как разрешаются коллизии?',
      answer: `**Хеш-таблица** хранит пары «ключ-значение» и даёт доступ по ключу в среднем за **O(1)**. Идея: **хеш-функция** превращает ключ в индекс массива (бакета): \`index = hash(key) % capacity\`.

**Коллизия** — когда два разных ключа дают один индекс. Способы разрешения:

- **Цепочки (separate chaining)** — в каждом бакете хранится список (или дерево) элементов. При коллизии добавляем в список. В Java \`HashMap\` при длинной цепочке (≥8) превращает её в сбалансированное дерево, улучшая худший случай до \`O(log n)\`.
- **Открытая адресация (open addressing)** — при коллизии ищем следующий свободный бакет (линейное/квадратичное пробирование, двойное хеширование). Экономит память на ссылках, но чувствительна к заполненности.

**Load factor** (заполненность) — отношение элементов к ёмкости. При превышении порога (в Java 0.75) таблица **расширяется** и все элементы **перехешируются** — дорогая операция \`O(n)\`, но амортизированно вставка остаётся \`O(1)\`.

**Худший случай** — \`O(n)\`, если все ключи попадают в один бакет (плохая хеш-функция или атака). Хорошая хеш-функция распределяет ключи равномерно.`,
    },
    {
      id: 'alg-stack-queue',
      difficulty: 'easy',
      tags: ['data-structures'],
      related: ['alg-array-vs-linkedlist', 'alg-tree-traversal'],
      question: 'Что такое стек и очередь и где они применяются?',
      answer: `Обе — линейные структуры, отличаются **порядком доступа**.

**Стек (Stack)** — **LIFO** (Last In, First Out): кто вошёл последним, выйдет первым. Основные операции \`push\` (положить наверх) и \`pop\` (снять сверху) — обе **O(1)**.

- где: стек вызовов функций, отмена (undo), проверка сбалансированности скобок, обход в глубину (DFS), вычисление выражений.

**Очередь (Queue)** — **FIFO** (First In, First Out): кто вошёл первым, выйдет первым. Операции \`enqueue\` (в конец) и \`dequeue\` (из начала) — **O(1)**.

- где: обработка задач по порядку, буферы, обход в ширину (BFS), очереди сообщений.

Варианты: **дек (Deque)** — вставка/удаление с обоих концов; **приоритетная очередь** — выдаёт не самый старый, а самый приоритетный элемент (реализуется кучей). Реализуют стек и очередь поверх массива или связного списка.`,
    },
    {
      id: 'alg-binary-search',
      difficulty: 'easy',
      tags: ['searching'],
      related: ['alg-common-complexities', 'alg-recursion'],
      question: 'Как работает бинарный поиск и какие у него требования?',
      answer: `**Бинарный поиск** находит элемент в **отсортированном** массиве за **O(log n)**, каждый раз вдвое сокращая область поиска.

Алгоритм: берём средний элемент; если он равен искомому — нашли; если искомое меньше — ищем в левой половине, если больше — в правой; повторяем, пока границы не сойдутся.

\`\`\`java
int binarySearch(int[] a, int target) {
    int lo = 0, hi = a.length - 1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2; // так не переполнится
        if (a[mid] == target) return mid;
        if (a[mid] < target) lo = mid + 1;
        else hi = mid - 1;
    }
    return -1; // не найдено
}
\`\`\`

**Требования и нюансы:**

- массив должен быть **отсортирован** — иначе поиск не работает;
- \`mid = lo + (hi - lo) / 2\` вместо \`(lo + hi) / 2\` — чтобы избежать переполнения int;
- легко ошибиться в границах (\`<=\` vs \`<\`, \`mid+1\`/\`mid-1\`) — классический источник багов.

Идея «отбрасывать половину» лежит в основе поиска по ответу (binary search on answer) и работы со сбалансированными деревьями.`,
    },
    {
      id: 'alg-sorting',
      difficulty: 'medium',
      tags: ['sorting'],
      related: ['alg-common-complexities', 'alg-heap'],
      question: 'Сравните основные алгоритмы сортировки. Что такое стабильность?',
      answer: `**Эффективные (O(n log n)) сортировки сравнением:**

- **Merge sort (слиянием)** — делит массив пополам, рекурсивно сортирует и сливает. Всегда \`O(n log n)\`, **стабильна**, но требует \`O(n)\` дополнительной памяти.
- **Quick sort (быстрая)** — выбирает опорный элемент (pivot), разбивает на «меньше/больше», рекурсивно сортирует части. В среднем \`O(n log n)\` и сортирует **на месте**, но худший случай \`O(n²)\` (плохой выбор pivot); обычно **нестабильна**. На практике часто самая быстрая.
- **Heap sort (пирамидальная)** — строит кучу и извлекает максимумы. \`O(n log n)\`, на месте, но нестабильна и хуже по кэшу.

**Простые (O(n²))** — пузырьковая, вставками, выбором. Медленные на больших данных, но сортировка **вставками** хороша на почти отсортированных и маленьких массивах.

**Стабильность** — сохранение относительного порядка **равных** элементов. Важно при сортировке по нескольким ключам (сначала по одному, потом стабильно по другому). \`Arrays.sort\` для примитивов в Java — quicksort (нестабилен, но для примитивов это неважно), для объектов — TimSort (стабилен).

**Нижняя граница** сортировки сравнением — \`O(n log n)\`. Быстрее (\`O(n)\`) можно только без сравнений: counting/radix sort — но лишь для ограниченных диапазонов ключей.`,
    },
    {
      id: 'alg-recursion',
      difficulty: 'easy',
      tags: ['basics'],
      related: ['alg-dp', 'alg-backtracking'],
      question: 'Что такое рекурсия? Роль базового случая и стека вызовов.',
      answer: `**Рекурсия** — приём, при котором функция вызывает саму себя, сводя задачу к более простой версии той же задачи.

Два обязательных элемента:

- **Базовый случай (base case)** — условие остановки, когда ответ известен без рекурсии. Без него — бесконечная рекурсия и **StackOverflowError**.
- **Рекурсивный шаг** — вызов себя на «уменьшенном» входе, приближающий к базовому случаю.

\`\`\`java
int factorial(int n) {
    if (n <= 1) return 1;      // базовый случай
    return n * factorial(n-1); // рекурсивный шаг
}
\`\`\`

**Стек вызовов:** каждый рекурсивный вызов кладёт кадр (аргументы, локальные переменные) на стек. Глубина рекурсии = высота стека → рекурсия тратит **O(глубины)** памяти. Слишком глубокая рекурсия переполняет стек.

**Рекурсия vs итерация:** любую рекурсию можно переписать циклом (иногда со своим стеком). Рекурсия часто читается яснее (деревья, «разделяй и властвуй»), но итерация экономнее по памяти. **Хвостовую рекурсию** некоторые языки оптимизируют в цикл — но **JVM этого не делает**.`,
    },
    {
      id: 'alg-tree-bst',
      difficulty: 'medium',
      tags: ['data-structures', 'trees'],
      related: ['alg-tree-traversal', 'alg-balanced-trees'],
      question: 'Что такое двоичное дерево поиска (BST)?',
      answer: `**Дерево** — иерархическая структура из узлов: корень, у каждого узла есть потомки; узлы без потомков — листья.

**Двоичное дерево** — у каждого узла не более двух потомков (левый и правый).

**Двоичное дерево поиска (BST)** добавляет **инвариант упорядоченности**: для любого узла все ключи в **левом** поддереве **меньше** его ключа, а в **правом** — **больше**.

Благодаря этому поиск, вставка и удаление работают как бинарный поиск — спускаемся влево или вправо:

- в **сбалансированном** дереве — **O(log n)**;
- в **вырожденном** (вставляли отсортированные данные) дерево превращается в связный список — **O(n)**.

**Обход in-order** (левый → корень → правый) даёт ключи в **отсортированном порядке** — полезное свойство BST.

Именно риск вырождения породил **самобалансирующиеся** деревья (AVL, красно-чёрные), которые гарантируют \`O(log n)\` в худшем случае. На BST построены \`TreeMap\`/\`TreeSet\` в Java (на красно-чёрном дереве).`,
    },
    {
      id: 'alg-tree-traversal',
      difficulty: 'medium',
      tags: ['trees', 'graphs'],
      related: ['alg-tree-bst', 'alg-graph-traversal'],
      question: 'Какие бывают обходы дерева?',
      answer: `Обходы делятся на **в глубину (DFS)** и **в ширину (BFS)**.

**DFS (в глубину)** — идём как можно глубже, потом возвращаемся. Три порядка для двоичного дерева:

- **in-order** (левый → корень → правый) — для BST даёт **отсортированный** список;
- **pre-order** (корень → левый → правый) — для копирования/сериализации дерева;
- **post-order** (левый → правый → корень) — для удаления дерева, подсчёта на основе детей.

Реализуется рекурсией или явным **стеком**.

**BFS (в ширину, level-order)** — обходим уровень за уровнем, сверху вниз. Реализуется **очередью**: кладём корень, затем на каждом шаге извлекаем узел и добавляем его детей.

\`\`\`
     1          in-order:   4 2 5 1 3
    / \\         pre-order:  1 2 4 5 3
   2   3        post-order: 4 5 2 3 1
  / \\           BFS:        1 2 3 4 5
 4   5
\`\`\`

Сложность любого обхода — **O(n)** (посещаем каждый узел один раз), память — O(высоты) для DFS и O(ширины) для BFS.`,
    },
    {
      id: 'alg-balanced-trees',
      difficulty: 'hard',
      tags: ['trees'],
      related: ['alg-tree-bst'],
      question: 'Зачем нужны самобалансирующиеся деревья (AVL, красно-чёрные)?',
      answer: `Обычный BST вырождается в список при неудачном порядке вставок (например, отсортированные данные), теряя своё преимущество — операции становятся **O(n)** вместо \`O(log n)\`.

**Самобалансирующиеся деревья** автоматически поддерживают малую высоту (\`O(log n)\`) при вставках/удалениях с помощью **поворотов** (rotations) — локальной перестройки узлов, сохраняющей инвариант BST.

- **AVL-дерево** — строго сбалансировано: разница высот поддеревьев любого узла ≤ 1. Даёт очень быстрый **поиск**, но чаще делает повороты при вставке/удалении.
- **Красно-чёрное дерево** — балансировка слабее (путь до листа не более чем вдвое длиннее кратчайшего), поэтому меньше поворотов и быстрее **вставка/удаление** ценой чуть большей высоты. Компромисс, который выбрали в стандартных библиотеках: Java \`TreeMap\`/\`TreeSet\`, C++ \`std::map\`.

Итог: гарантированные **O(log n)** для поиска, вставки и удаления в худшем случае — то, чего не даёт наивный BST. Альтернатива деревьям для быстрого доступа — хеш-таблицы (в среднем \`O(1)\`, но без упорядоченности и с худшим случаем \`O(n)\`).`,
    },
    {
      id: 'alg-heap',
      difficulty: 'medium',
      tags: ['data-structures', 'trees'],
      related: ['alg-sorting', 'alg-stack-queue'],
      question: 'Что такое куча (heap) и приоритетная очередь?',
      answer: `**Куча (binary heap)** — двоичное дерево, удовлетворяющее **свойству кучи**:

- **min-heap** — ключ каждого узла ≤ ключей его детей (в корне — минимум);
- **max-heap** — ключ каждого узла ≥ ключей детей (в корне — максимум).

Это **полное** дерево, поэтому его компактно хранят в **массиве**: для узла с индексом \`i\` дети — \`2i+1\` и \`2i+2\`, родитель — \`(i-1)/2\`.

Операции:

- \`peek\` (посмотреть минимум/максимум) — **O(1)**;
- \`insert\` (добавить, «всплытие» вверх) — **O(log n)**;
- \`extract\` (извлечь корень, «просеивание» вниз) — **O(log n)**;
- построить кучу из массива — **O(n)**.

**Приоритетная очередь** — абстракция «выдать самый приоритетный элемент», чаще всего реализуемая кучей (в Java — \`PriorityQueue\`).

**Где применяется:** heap sort, алгоритм Дейкстры, задача «top-K элементов», планировщики задач, слияние k отсортированных списков. Куча не поддерживает быстрый поиск произвольного элемента — только доступ к экстремуму.`,
    },
    {
      id: 'alg-graph-representation',
      difficulty: 'medium',
      tags: ['graphs'],
      related: ['alg-graph-traversal'],
      question: 'Как представляют граф в памяти?',
      answer: `**Граф** — вершины (nodes) и рёбра (edges). Бывает **ориентированным/неориентированным** и **взвешенным/невзвешенным**. Два основных представления:

**Список смежности (adjacency list)** — для каждой вершины хранится список её соседей.

- память — **O(V + E)** (V вершин, E рёбер);
- проверка «есть ли ребро u→v» — O(степени u);
- перебор соседей вершины — эффективен;
- ➕ экономно для **разреженных** графов (рёбер немного) — это большинство реальных графов.

**Матрица смежности (adjacency matrix)** — таблица V×V, где ячейка [i][j] = 1 (или вес), если есть ребро.

- память — **O(V²)** независимо от числа рёбер;
- проверка «есть ли ребро» — **O(1)**;
- ➕ удобна для **плотных** графов и алгоритмов, которым нужен быстрый доступ к любому ребру.

**Выбор:** почти всегда берут **список смежности** (реальные графы разрежены; V² быстро становится неподъёмным). Матрицу — когда граф плотный или нужен постоянный доступ к ребру (например, алгоритм Флойда-Уоршелла).`,
    },
    {
      id: 'alg-graph-traversal',
      difficulty: 'medium',
      tags: ['graphs'],
      related: ['alg-graph-representation', 'alg-shortest-path'],
      question: 'Чем BFS отличается от DFS на графе?',
      answer: `Оба обходят все достижимые вершины за **O(V + E)** (со списком смежности), но по-разному и с разным применением. В графе (в отличие от дерева) обязательно помечать **посещённые** вершины, иначе циклы приведут к бесконечному обходу.

**BFS (поиск в ширину)** — очередь, обход по «слоям» (все соседи, потом соседи соседей).

- находит **кратчайший путь по числу рёбер** в невзвешенном графе;
- память — до **O(V)** (хранит целый «фронт»);
- применения: кратчайший путь в невзвешенном графе, поиск ближайших, уровни/расстояния.

**DFS (поиск в глубину)** — стек (или рекурсия), идём вглубь до упора, потом возвращаемся.

- память — **O(глубины)**;
- применения: проверка связности, **обнаружение циклов**, **топологическая сортировка**, поиск компонент связности, задачи на бэктрекинг.

Кратко: нужен **кратчайший путь без весов** или послойный обход → BFS; нужно «пройти все пути»/структурные свойства (циклы, топосорт, компоненты) → DFS.`,
    },
    {
      id: 'alg-shortest-path',
      difficulty: 'hard',
      tags: ['graphs'],
      related: ['alg-graph-traversal', 'alg-heap'],
      question: 'Как искать кратчайший путь в графе?',
      answer: `Выбор алгоритма зависит от весов рёбер:

- **Невзвешенный граф** — обычный **BFS** от источника даёт кратчайший путь по числу рёбер за \`O(V + E)\`.
- **Взвешенный граф с неотрицательными весами** — **алгоритм Дейкстры**: жадно выбираем ближайшую ещё не обработанную вершину (через **приоритетную очередь**/кучу), релаксируя рёбра. Сложность \`O((V + E) log V)\`. Не работает с отрицательными весами.
- **Есть отрицательные веса** — **Беллман-Форд**: \`O(V·E)\`, медленнее, но обрабатывает отрицательные рёбра и **обнаруживает отрицательные циклы**.
- **Кратчайшие пути между всеми парами** — **Флойд-Уоршелл**: \`O(V³)\`, прост, годится для небольших плотных графов.
- **A\\*** — эвристическое ускорение Дейкстры, когда известна оценка расстояния до цели (например, на картах).

**Релаксация** — общая идея: если через вершину u путь до v короче известного, обновляем расстояние до v. Дейкстра — самый частый ответ на собеседовании для «кратчайший путь с весами».`,
    },
    {
      id: 'alg-two-pointers',
      difficulty: 'medium',
      tags: ['patterns'],
      related: ['alg-sliding-window', 'alg-binary-search'],
      question: 'В чём суть техники двух указателей (two pointers)?',
      answer: `**Два указателя** — приём, где по структуре (обычно массиву/строке) движутся два индекса, заменяя вложенный цикл и снижая сложность с \`O(n²)\` до **O(n)**.

Основные схемы:

- **Навстречу друг другу** — один указатель с начала, другой с конца, сходятся к середине. Классика: проверка палиндрома; поиск пары с заданной суммой в **отсортированном** массиве (двигаем нужный конец в зависимости от суммы).
- **Быстрый и медленный (fast/slow)** — идут с разной скоростью. Обнаружение цикла в связном списке (алгоритм Флойда «черепаха и заяц»), поиск середины списка.
- **Одинаковое направление** — часто основа скользящего окна.

\`\`\`java
// пара с суммой target в отсортированном массиве
int lo = 0, hi = a.length - 1;
while (lo < hi) {
    int sum = a[lo] + a[hi];
    if (sum == target) return new int[]{lo, hi};
    if (sum < target) lo++; else hi--;
}
\`\`\`

Условие применимости: часто нужен **отсортированный** вход или свойство, позволяющее осмысленно двигать указатели. Экономит и время, и память (\`O(1)\` дополнительной).`,
    },
    {
      id: 'alg-sliding-window',
      difficulty: 'medium',
      tags: ['patterns'],
      related: ['alg-two-pointers'],
      question: 'Что такое техника скользящего окна (sliding window)?',
      answer: `**Скользящее окно** — приём для задач о **непрерывном подотрезке** (подстроке/подмассиве): вместо перебора всех подотрезков за \`O(n²)\` поддерживаем «окно» [left, right] и двигаем его за **O(n)**.

Идея: расширяем окно, сдвигая **right**; когда нарушается условие — сжимаем, сдвигая **left**. Каждый элемент входит и выходит из окна не более одного раза → линейно.

Два вида:

- **Фиксированного размера** — например, максимальная сумма подмассива длины k: добавляем новый элемент справа, убираем старый слева.
- **Переменного размера** — например, самая длинная подстрока без повторяющихся символов: расширяем, пока нет дубликата; при дубликате сжимаем слева.

\`\`\`java
// макс. сумма подмассива длины k
int sum = 0, best = Integer.MIN_VALUE;
for (int right = 0; right < a.length; right++) {
    sum += a[right];
    if (right >= k - 1) { best = Math.max(best, sum); sum -= a[right - k + 1]; }
}
\`\`\`

Признак задачи под скользящее окно: «найдите самый длинный/короткий/оптимальный **непрерывный** фрагмент, удовлетворяющий условию». Тесно связано с техникой двух указателей.`,
    },
    {
      id: 'alg-dp',
      difficulty: 'hard',
      tags: ['patterns'],
      related: ['alg-recursion', 'alg-greedy'],
      question: 'Что такое динамическое программирование (DP)?',
      answer: `**Динамическое программирование** решает задачу, разбивая её на **перекрывающиеся подзадачи** и переиспользуя их результаты, чтобы не считать одно и то же многократно.

Два признака, что задача решается DP:

- **Оптимальная подструктура** — оптимум задачи выражается через оптимумы подзадач;
- **Перекрывающиеся подзадачи** — одни и те же подзадачи встречаются много раз (иначе это обычное «разделяй и властвуй»).

Два подхода:

- **Top-down (мемоизация)** — обычная рекурсия + кэш результатов подзадач. Считаем лениво, только нужное.
- **Bottom-up (табуляция)** — заполняем таблицу от базовых случаев к искомому итеративно. Экономит стек, часто позволяет уменьшить память.

\`\`\`java
// Фибоначчи: наивно O(2^n), с DP — O(n)
int fib(int n) {
    int[] dp = new int[n + 1];
    dp[1] = 1;
    for (int i = 2; i <= n; i++) dp[i] = dp[i-1] + dp[i-2];
    return dp[n];
}
\`\`\`

Классические задачи: рюкзак (knapsack), наибольшая общая подпоследовательность, размен монет, редакционное расстояние. Суть DP — **«запомнить, чтобы не пересчитывать»**: превращает экспоненту в полином.`,
    },
    {
      id: 'alg-greedy',
      difficulty: 'medium',
      tags: ['patterns'],
      related: ['alg-dp'],
      question: 'Что такое жадные алгоритмы и чем они отличаются от DP?',
      answer: `**Жадный алгоритм** на каждом шаге делает **локально оптимальный** выбор в надежде получить глобальный оптимум — и никогда не пересматривает решения.

Работает **корректно** только если у задачи есть **свойство жадного выбора**: локально лучший выбор ведёт к глобально оптимальному решению. Это нужно **доказывать** — иначе жадность даёт неверный ответ.

**Примеры, где жадность работает:**

- размен монет каноничными номиналами (1, 5, 10…) — брать всегда наибольшую;
- задача о выборе непересекающихся интервалов (activity selection) — брать заканчивающийся раньше;
- коды Хаффмана, минимальное остовное дерево (Прим/Краскал), Дейкстра.

**Жадность vs DP:**

- **жадный** — один проход, никогда не возвращается, \`O(n)\`/\`O(n log n)\`, но применим не всегда;
- **DP** — рассматривает все варианты подзадач, надёжнее, но дороже по времени/памяти.

Пример разницы: размен **произвольными** номиналами (например, 1, 3, 4 на сумму 6) жадность решает неверно (4+1+1=3 монеты вместо 3+3=2) — нужен DP. Всегда проверяйте, действительно ли жадный выбор оптимален.`,
    },
    {
      id: 'alg-backtracking',
      difficulty: 'hard',
      tags: ['patterns'],
      related: ['alg-recursion', 'alg-dp'],
      question: 'Что такое бэктрекинг (поиск с возвратом)?',
      answer: `**Бэктрекинг** — систематический перебор кандидатов решения, при котором мы строим решение по шагам и **откатываемся** (backtrack), как только понимаем, что текущий частичный вариант не может привести к цели.

Отличие от полного перебора — **отсечение (pruning)**: тупиковые ветви отбрасываются рано, что резко сокращает пространство поиска.

Общая схема (рекурсивная):

\`\`\`
solve(состояние):
    если состояние — полное решение: записать его
    иначе для каждого допустимого следующего шага:
        сделать шаг               // выбрать
        solve(новое состояние)    // рекурсия
        отменить шаг              // backtrack (откат)
\`\`\`

**Классические задачи:** расстановка N ферзей, судоку, генерация всех перестановок/подмножеств/комбинаций, поиск пути в лабиринте, раскраска графа.

Сложность обычно экспоненциальная (перебор), но отсечения делают её практичной на реальных входах. Бэктрекинг строится на **рекурсии**; когда среди переборных подзадач много перекрытий и нужен оптимум — задачу часто переводят в **динамическое программирование**.`,
    },
  ],
};
